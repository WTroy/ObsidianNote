---
title: 6. 图
source: 有道云笔记
imported: true
---

题⽬⼀：
图的存储⽅式：
如何表达图？⽣成图？
⼤图的结构：包括点集和边集：
邻接表
邻接矩阵
/**1
* 图的结构：包括点集和边集2
*3
* @author wanghao4

*/5
public class Graph {6
public HashMap<Integer, Node> nodes;7
public HashSet<Edge> edges;8
9
public Graph(HashMap<Integer, Node> nodes, HashSet<Edge> edges) {10
this.nodes = nodes;11
this.edges = edges;12
}13
}14
/**15
* 节点16
*17
* @author wanghao18
*/19
public class Node {20
public int value;21
public int in;22
public int out;23
public ArrayList<Node> nexts;24
public ArrayList<Edge> edges;25
26
public Node(int value) {27
this.value = value;28
in = 0;29
out = 0;30
nexts = new ArrayList<>();31
edges = new ArrayList<>();32
}33
}34
/**35
* 边36
* @author wanghao37
*/38
public class Edge {39
public int weight;40
public Node from;41
public Node to;42
43
public Edge(int weight, Node from, Node to) {44
this.weight = weight;45
this.from = from;46

⽣成图的过程实现：
题⽬⼆：
图的宽度优先遍历：
this.to = to;47
}48
}49
/**1
* ⼀个⼆维数组  matrix 数组是 N*3 的⼤⼩2
* N 表示每⼀⾏，代表⼀个元素3
* 3 分别表示  出发节点的值  到达节点的值  路径⻓度（权重）4
* [from 节点的值， to 节点的值， weoght]5
*6
*/7
public static Graph createGraph(Integer[][] matrix) {8
Graph graph = new Graph();9
for (int i = 0; i < matrix.length; i++) {10
// 当 i=0 时  matrix[0][0] matrix[0][1] matrix[0][2]11
Integer from = matrix[i][0];12
Integer to = matrix[i][1];13
Integer weight = matrix[i][2];14
if (!graph.nodes.containsKey(from)) {15
graph.nodes.put(from, new Node(from));16
}17
if (!graph.nodes.containsKey(to)) {18
graph.nodes.put(to, new Node(to));19
}20
Node fromNode = graph.nodes.get(from);21
Node toNode = graph.nodes.get(to);22
Edge newEdge = new Edge(weight, fromNode, toNode);23
fromNode.nexts.add(toNode);24
fromNode.out++;25
toNode.in++;26
fromNode.edges.add(newEdge);27
graph.edges.add(newEdge);28
}29
return graph;30
}31

深度优先遍历
以下实现代码可以使⽤数组代替 hash
利⽤队列实现
从原节点开始依次按照宽度进队列，然后弹出
每弹出⼀个点，把该节点所有没有进过队列的邻接点放⼊队列
直到队列为空
利⽤栈实现
从原节点开始把节点按照深度放⼊栈，然后弹出
每弹出⼀个点，把该节点下⼀个没有进过栈的邻节点放⼊栈
直到栈变空
/**1
* 图的宽（⼴）度优先遍历2
* 利⽤队列实现： FIFO3
* set 是为了去重4
* @author wanghao5
*/6
public class Code01_BFS {7
public static void bfs(Node node) {8
if (node == null) {9
return;10
}11
Queue<Node> queue = new LinkedList<>();12
HashSet<Node> set = new HashSet<>();13
queue.add(node);14
set.add(node);15
while (!queue.isEmpty()) {16
Node cur = queue.poll();17
System.out.println(cur.value);18
for (Node next : cur.nexts) {19
if (!set.contains(next)) {20
set.add(next);21
queue.add(next);22
}23
}24
}25
}26
}27

深度优先遍历：
/**1
* 图的深度优先遍历2
* 利⽤栈来实现3
*4
* @author wanghao5
*/6
public class Code02_DFS {7
public static void dfs(Node node) {8
if (node == null) {9
return;10
}11
Stack<Node> stack = new Stack<>();12
HashSet<Node> set = new HashSet<>();13
stack.add(node);14
set.add(node);15
System.out.println(node.value);16
while (!stack.isEmpty()) {17
Node cur = stack.pop();18
for (Node next : cur.nexts) {19
if (!set.contains(next)) {20
stack.push(cur);21
stack.push(next);22
set.add(next);23
System.out.println(next.value);24

题⽬三：
拓扑排序算法
适⽤范围：要求有向图，且有⼊度为 0 的节点，且没有环
拓扑排序（ Topological Sorting ）
若⼀个由图中所有点构成的序列  A 满⾜：对于图中的每条边  (x,y) ， x 在  A 中都出现在  y 之前，则称  A 是该图的⼀
个拓扑序列。
且该序列必须满⾜下⾯两个条件：
1. 每个顶点出现且只出现⼀次。
2.  若存在⼀条从顶点  x 到顶点  y 的路径，那么在序列中顶点  x 出现在顶点  y 的前⾯。
拓扑排序只适⽤于  AOV ⽹  （有向⽆环图）
若图中有环，则⼀定不存在拓扑序。
可以证明，⼀个有向⽆环图，⼀定存在⼀个拓扑序列。有向⽆环图，⼜被称为拓扑图。
⼊度 : 即有多少条边指向⾃⼰这个节点。
出度 : 即有多少条边从⾃⼰这个节点指出去。
// break ⼗分关键，他保证了按照同⼀条线向下执⾏25
break;26
}27
}28
}29
}30
}31
/**1
* 拓扑排序算法2
*3
* @author wanghao4
*/5
public class Code3_TopologySort {6
public static List<Node> sortedTopology(Graph graph) {7
// key: 某⼀个 node8
// value: 剩余的⼊度9
HashMap<Node, Integer> inMap = new HashMap<>();10
// ⼊度为 0 的点，才能进这个队列11
Queue<Node> zeroInQueue = new LinkedList<>();12
for (Node node : graph.nodes.values()) {13
inMap.put(node, node.in);14
if (node.in == 0) {15
zeroInQueue.add(node);16

题⽬四：
kruskal 算法
适⽤范围：要求⽆向图
克鲁斯卡尔算法是求连通⽹的最⼩⽣成树的另⼀种⽅法。与不同，它的时间复杂度为 O （ eloge ）（ e 为
⽹中的边数），所以，适合于求边稀疏的⽹的最⼩⽣成树
最⼩⽣成树：
从最⼩的权重边开始加，如果产⽣环则不增加这个边（如何判断产⽣环）：
}17
}18
// 拓扑排序的结果，依次加⼊ result19
List<Node> result = new ArrayList<>();20
while (!zeroInQueue.isEmpty()) {21
Node cur = zeroInQueue.poll();22
result.add(cur);23
for (Node next : cur.nexts) {24
inMap.put(next, inMap.get(next) - 1);25
if (inMap.get(next) == 0) {26
zeroInQueue.add(next);27
}28
}29
}30
return result;31
}32
}33
34
普⾥姆算法

/**1
* @author wanghao2
*  kruskal 算法原理3
* 现在我们假设⼀个图有 m 个节点， n 条边。⾸先，我们需要把 m 个节点看成 m 个独⽴的⽣成树，并且把 n 条边按
照从⼩到⼤的数据进⾏排列。在 n 条边中，我们依次取出其中的每⼀条边，如果发现边的两个节点分别位于两棵
树上，那么把两棵树合并成为⼀颗树；如果树的两个节点位于同⼀棵树上，那么忽略这条边，继续运⾏。等到所
有的边都遍历结束之后，如果所有的⽣成树可以合并成⼀条⽣成树，那么它就是我们需要寻找的最⼩⽣成树，反
之则没有最⼩⽣成树。
4
*/5
public class UnionFind {6
public HashMap<Node, List<Node>> setMap;7
8
public void makeSets(List<Node> nodes){9
for (Node cur: nodes){10
List<Node> set = new ArrayList<>();11
set.add(cur);12
setMap.put(cur, set);13
}14
}15
16
public boolean isSameSet(Node from, Node to){17
List<Node> fromSet = setMap.get(from);18
List<Node> toSet = setMap.get(to);19
return fromSet==toSet;20
}21

Kruskal 克鲁斯卡尔算法：
public void union(Node from, Node to){22
List<Node> fromSet = setMap.get(from);23
List<Node> toSet = setMap.get(to);24
for (Node toNode: toSet){25
fromSet.add(toNode);26
setMap.put(toNode, fromSet);27
}28
}29
}30
/**1
* 权重的⽐较器2
* @author wanghao3
*/4
public class EdgeComparator implements Comparator<Edge> {5
@Override6
public int compare(Edge o1, Edge o2) {7
return o1.weight - o2.weight;8
}9

题⽬五：
prim 算法：（贪⼼）
适⽤范围：要求⽆向图
Prim 算法原理：
1 ）以某⼀个点开始，寻找当前该点可以访问的所有的边；
2 ）在已经寻找的边中发现最⼩边，这个边必须有⼀个点还没有访问过，将还没有访问的点加⼊我们的集合，记录添加
的边；
3 ）寻找当前集合可以访问的所有边，重复 2 的过程，直到没有新的点可以加⼊；
4 ）此时由所有边构成的树即为最⼩⽣成树。
}10
11
public class KruskalDemo {12
public static Set<Edge> kruskalMST(Graph graph) {13
UnionFind unionFind = new UnionFind();14
unionFind.makeSets(new ArrayList<>(graph.nodes.values()));15
PriorityQueue<Edge> priorityQueue = new PriorityQueue<>(new
EdgeComparator());
16
for (Edge edge : graph.edges) { // M 条边17
priorityQueue.add(edge); // O(logM)18
}19
Set<Edge> result = new HashSet<>();20
while (!priorityQueue.isEmpty()) { // M 条边21
Edge edge = priorityQueue.poll(); // O(logM)22
if (!unionFind.isSameSet(edge.from, edge.to)) { // O(1)23
result.add(edge);24
unionFind.union(edge.from, edge.to);25
}26
}27
return result;28
}29
}30
Prim_ 百度百科  (baidu.com)

public static Set<Edge> primMST(Graph graph) {1
// 解锁的边进⼊⼩根堆2
PriorityQueue<Edge> priorityQueue = new PriorityQueue<>(new
EdgeComparator());
3
HashSet<Node> set = new HashSet<Node>();4

总的来说
Prim 算法是以点为对象，挑选与点相连的最短边来构成最⼩⽣成树。⽽ Kruskal 算法是以边为对象，不断
地加⼊新的不构成环路的最短边来构成最⼩⽣成树。
题⽬六：
Dijkstra 算法
适⽤范围：没有权值为负的边
单元最短路径算法：
Set<Edge> result = new HashSet<>();5
for (Node node : graph.nodes.values()) { // 随便挑⼀个节点6
// node 是开始点7
if (!set.contains(node)) {8
set.add(node);9
for (Edge edge : node.edges) { // 由⼀个节点，解锁所有相连的边10
priorityQueue.add(edge);11
}12
while (!priorityQueue.isEmpty()) {13
Edge edge = priorityQueue.poll(); // 弹出解锁的边中最⼩的边14
Node toNode = edge.to; // 可能的⼀个新的节点15
if (!set.contains(toNode)) { // 不包含的话就是新的节点16
set.add(toNode);17
result.add(edge);18
for (Edge nextEdge : toNode.edges) {19
priorityQueue.add(nextEdge);20
}21
}22
}23
}24
break;25
}26
return result;27
}28

刚开始从 A 出发，除了 A ⾃⼰，其他距离都记做⽆穷⼤；更新后选最⼩距离的 B ，再从 B 出发，如果距离变的更短就更
新。
public static HashMap<Node, Integer> dijkstra1(Node head) {1
/**2

* 从 head 出发到所有点的最⼩距离3
* key ：从 head 出发到达 key4
* value ：从 head 出发到达 key 的最⼩距离5
* 如果在表中，没有 T 的记录，含义是从 head 出发到 T 这个点的距离是正⽆穷6
*/7
HashMap<Node, Integer> distanceMap = new HashMap<>();8
distanceMap.put(head, 0);9
// 已经求过距离的节点，存在 selectedNodes 中，以后再也不碰10
HashSet<Node> selectedNodes = new HashSet<>();11
Node minNode = getMinDistanceAndUnseletedNode(distanceMap, selectedNodes);12
while (minNode != null) {13
int distance = distanceMap.get(minNode);14
for (Edge edge : minNode.edges) {15
Node toNode = edge.to;16
if (!distanceMap.containsKey(toNode)) {17
distanceMap.put(toNode, distance + edge.weight);18
}19
distanceMap.put(edge.to, Math.min(distanceMap.get(toNode), distance
+ edge.weight));
20
}21
selectedNodes.add(minNode);22
minNode = getMinDistanceAndUnseletedNode(distanceMap, selectedNodes);23
}24
return distanceMap;25
}26
27
private static Node getMinDistanceAndUnseletedNode(HashMap<Node, Integer>
distanceMap, HashSet<Node> selectedNodes) {
28
Node minNode = null;29
int minDistance = Integer.MAX_VALUE;30
for (Map.Entry<Node, Integer> entry : distanceMap.entrySet()) {31
Node node = entry.getKey();32
int distance = entry.getValue();33
if (!selectedNodes.contains(node) && distance < minDistance) {34
minNode = node;35
minDistance = distance;36
}37
}38
return minNode;39
}40
41
public static class NodeRecord {42
public Node node;43

利⽤⼩根堆来优化这个过程：
需要⾃⼰修改⼩根堆的操作，系统默认提供的⼩根堆⽆法实现修改完某⼀个位置的值之后继续维持⼩根堆结构，系统默
认功能只从堆顶去掉⼀个元素之后其他的值继续保持⼩根堆。
public int distance;44
45
public NodeRecord(Node node, int distance) {46
this.node = node;47
this.distance = distance;48
}49
}50
/**1
* 利⽤⼩根堆优化后的流程2
* 需⾃定义⼩根堆的操作 : 修改某个位置后仍然保持⼩根堆结构3
*4
* 从 head 出发，所有 head 能够到达的节点，⽣成到达每个节点的最⼩路径记录并返回5
*/6
public static class NodeRecord {7
public Node node;8
public int distance;9
10
public NodeRecord(Node node, int distance) {11
this.node = node;12
this.distance = distance;13
}14
}15
16
public static HashMap<Node, Integer> dijkstra2(Node head, int size) {17
NodeHeap nodeHeap = new NodeHeap(size);18
nodeHeap.addOrUpdateOrIgnore(head, 0);19
HashMap<Node, Integer> result = new HashMap<>();20
while (!nodeHeap.isEmpty()) {21
NodeRecord record = nodeHeap.pop();22
Node cur = record.node;23
int distance = record.distance;24
for (Edge edge : cur.edges) {25
nodeHeap.addOrUpdateOrIgnore(edge.to, edge.weight + distance);26
}27
result.put(cur, distance);28

}29
return result;30
}31
32
public static class NodeHeap {33
private Node[] nodes;34
private HashMap<Node, Integer> heapIndexMap;35
private HashMap<Node, Integer> distanceMap;36
private int size;37
38
public NodeHeap(int size) {39
nodes = new Node[size];40
heapIndexMap = new HashMap<>();41
distanceMap = new HashMap<>();42
this.size = 0;43
}44
45
public boolean isEmpty() {46
return size == 0;47
}48
49
public void addOrUpdateOrIgnore(Node node, int distance) {50
if (inHeap(node)) {51
distanceMap.put(node, Math.min(distanceMap.get(node), distance));52
insertHeapify(node, heapIndexMap.get(node));53
}54
if (!isEntered(node)) {55
nodes[size] = node;56
heapIndexMap.put(node, size);57
distanceMap.put(node, distance);58
insertHeapify(node, size++);59
}60
}61
62
public NodeRecord pop() {63
NodeRecord nodeRecord = new NodeRecord(nodes[0],
distanceMap.get(nodes[0]));
64
swap(0, size - 1);65
heapIndexMap.put(nodes[size - 1], -1);66
distanceMap.remove(nodes[size - 1]);67
nodes[size - 1] = null;68
heapify(0, --size);69

return nodeRecord;70
}71
72
private void insertHeapify(Node node, int index) {73
while (distanceMap.get(nodes[index]) < distanceMap.get(nodes[(index - 1)
/ 2])) {
74
swap(index, (index - 1) / 2);75
index = (index - 1) / 2;76
}77
}78
79
private void heapify(int index, int size) {80
int left = index * 2 + 1;81
while (left < size) {82
int smallest = left + 1 < size && distanceMap.get(nodes[left + 1]) <
distanceMap.get(nodes[left])
83
? left + 1 : left;84
smallest = distanceMap.get(nodes[smallest]) <
distanceMap.get(nodes[index]) ? smallest : index;
85
if (smallest == index) {86
break;87
}88
swap(smallest, index);89
index = smallest;90
left = index * 2 + 1;91
}92
}93
94
private boolean isEntered(Node node) {95
return heapIndexMap.containsKey(node);96
}97
98
private boolean inHeap(Node node) {99
return isEntered(node) && heapIndexMap.get(node) != -1;100
}101
102
private void swap(int index1, int index2) {103
heapIndexMap.put(nodes[index1], index2);104
heapIndexMap.put(nodes[index2], index1);105
Node tmp = nodes[index1];106
nodes[index1] = nodes[index2];107
nodes[index2] = tmp;108
}109

题⽬九：
N 皇后问题是指在 N*N 的棋盘上要摆 N 个皇后，要求任何两个皇后不同⾏，不同列，也不在同⼀条斜线上。
给定⼀个整数 n ，返回 n 皇后的摆法有多少种。
n=1, 返回 1
n=2 或 3 ， 2 皇后和 3 皇后问题⽆论怎么摆都不⾏，返回 0
n=8 ，返回 92
}110
public static int num1(int n) {1
if (n < 1) {2
return 0;3
}4
int[] record = new int[n]; // record[i] -> i ⾏的皇后，放在了第⼏列5
return process1(0, record, n);6
}7
/**8
* 潜台词： record[0...i-1] 的皇后，任何两个皇后⼀定都不同⾏，不同列，不公斜线9
* ⽬前来到了第 i ⾏10
* record[0...i-1] 表示之前的⾏，放了皇后的位置11
* n 代表整体⼀共有多少⾏12
* 返回值是，摆完所有的皇后，合理的摆法有多少种13
*/14

利⽤位运算进⾏优化 (y1s1 有点没看懂 ) ：
private static int process1(int i, int[] record, int n) {15
if (i == n) { // 终⽌⾏16
return 1;17
}18
int res = 0;19
for (int j = 0; j < n; j++) { // 当前⾏在 i ⾏，尝试 i ⾏所有的列  -> j20
// 当前 i ⾏的皇后，放在 j 列，会不会和之前 (0...i-1) 的皇后，不同⾏同列或者同斜线21
// 如果是，认为有效22
// 如果不是，认为⽆效23
if (isValid(record, i, j)) {24
record[i] = j;25
res += process1(i + 1, record, n);26
}27
}28
return res;29
}30
31
/**32
* record[0...i-1] 你需要看， record[i...] 不需要看33
* 返回 i ⾏皇后，放在了 j 列，是否有效34
*/35
private static boolean isValid(int[] record, int i, int j) {36
for (int k = 0; k < i; k++) {37
if (j == record[k] || Math.abs(record[k] - j) == Math.abs(i - k)) {38
return false;39
}40
}41
return true;42
}43
/**1
* 利⽤位运算来优化2
* 不要超过 32 皇后问题3
*/4
public static int num2(int n) {5
if (n < 1 || n > 32) {6
return 0;7
}8

int limit = n == 32 ? -1 : (1 << n) - 1;9
return process2(limit, 0, 0, 0);10
}11
12
/**13
* colLim 列的限制， 1 的位置不能放皇后， 0 的位置可以14
* leftDiaLim 左斜线的限制， 1 的位置不能放皇后， 0 的位置可以15
* rightDiaLim 右斜线的限制， 1 的位置不能放皇后， 0 的位置可以16
*/17
private static int process2(int limit, int colLim, int leftDiaLim,18
int rightDiaLim) {19
if (colLim == limit) { // base case20
return 1;21
}22
int pos = 0;23
int mostRightOne = 0;24
pos = limit & (~(colLim | leftDiaLim | rightDiaLim));25
int res = 0;26
while (pos != 0) {27
mostRightOne = pos & (~pos + 1);28
pos = pos - mostRightOne;29
res += process2(limit, colLim | mostRightOne,30
(leftDiaLim | mostRightOne) << 1,31
(rightDiaLim | mostRightOne) >>> 1);32
33
}34
return res;35
}36