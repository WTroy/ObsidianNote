---
title: 6. 图
source: 有道云笔记
imported: true
---

题⽬⼀：
图的存储⽅式：
如何表达图？⽣成图？
⼤图的结构：包括点集和边集：
邻接表
邻接矩阵
/**1
* 图的结构：包括点集和边集2
*3
* @author wanghao4

*/5
public class Graph {6
public HashMap<Integer, Node> nodes;7
public HashSet<Edge> edges;8
public Graph(HashMap<Integer, Node> nodes, HashSet<Edge> edges) {10

```yaml
this.nodes = nodes;
this.edges = edges;
}
}
/**
```

* 节点16
*17
* @author wanghao18
*/19
public class Node {20
public int value;21
public int in;22
public int out;23
public ArrayList<Node> nexts;24
public ArrayList<Edge> edges;25
public Node(int value) {27

```java
this.value = value;
in = 0;
out = 0;
nexts = new ArrayList<>();
edges = new ArrayList<>();
}
}
/**
```

* 边36
* @author wanghao37
*/38
public class Edge {39
public int weight;40
public Node from;41
public Node to;42
public Edge(int weight, Node from, Node to) {44

```sql
this.weight = weight;
this.from = from;
```

⽣成图的过程实现：
题⽬⼆：
图的宽度优先遍历：

```yaml
this.to = to;
}
}
/**
```

* ⼀个⼆维数组  matrix 数组是 N*3 的⼤⼩2
* N 表示每⼀⾏，代表⼀个元素3
* 3 分别表示  出发节点的值  到达节点的值  路径⻓度（权重）4
* [from 节点的值， to 节点的值， weoght]5
*6
*/7
public static Graph createGraph(Integer[][] matrix) {8

```java
Graph graph = new Graph();
for (int i = 0; i < matrix.length; i++) {
```

// 当 i=0 时  matrix[0][0] matrix[0][1] matrix[0][2]11

```sql
Integer from = matrix[i][0];
Integer to = matrix[i][1];
Integer weight = matrix[i][2];
if (!graph.nodes.containsKey(from)) {
graph.nodes.put(from, new Node(from));
}
if (!graph.nodes.containsKey(to)) {
graph.nodes.put(to, new Node(to));
}
Node fromNode = graph.nodes.get(from);
Node toNode = graph.nodes.get(to);
Edge newEdge = new Edge(weight, fromNode, toNode);
fromNode.nexts.add(toNode);
fromNode.out++;
toNode.in++;
fromNode.edges.add(newEdge);
graph.edges.add(newEdge);
}
return graph;
}
```

深度优先遍历
以下实现代码可以使⽤数组代替 hash
利⽤队列实现
从原节点开始依次按照宽度进队列，然后弹出
每弹出⼀个点，把该节点所有没有进过队列的邻接点放⼊队列
直到队列为空
利⽤栈实现
从原节点开始把节点按照深度放⼊栈，然后弹出
每弹出⼀个点，把该节点下⼀个没有进过栈的邻节点放⼊栈
直到栈变空
/**1
* 图的宽（⼴）度优先遍历2
* 利⽤队列实现： FIFO3
* set 是为了去重4
* @author wanghao5
*/6
public class Code01_BFS {7
public static void bfs(Node node) {8
if (node == null) {9
return;10

```sql
}
Queue<Node> queue = new LinkedList<>();
HashSet<Node> set = new HashSet<>();
queue.add(node);
set.add(node);
while (!queue.isEmpty()) {
Node cur = queue.poll();
System.out.println(cur.value);
for (Node next : cur.nexts) {
if (!set.contains(next)) {
set.add(next);
queue.add(next);
}
}
}
}
}
```

深度优先遍历：
/**1
* 图的深度优先遍历2
* 利⽤栈来实现3
*4
* @author wanghao5
*/6
public class Code02_DFS {7
public static void dfs(Node node) {8
if (node == null) {9
return;10

```sql
}
Stack<Node> stack = new Stack<>();
HashSet<Node> set = new HashSet<>();
stack.add(node);
set.add(node);
System.out.println(node.value);
while (!stack.isEmpty()) {
Node cur = stack.pop();
for (Node next : cur.nexts) {
if (!set.contains(next)) {
stack.push(cur);
stack.push(next);
set.add(next);
System.out.println(next.value);
```

题⽬三：
拓扑排序算法
适⽤范围：要求有向图，且有⼊度为 0 的节点，且没有环
拓扑排序（ Topological Sorting ）
若⼀个由图中所有点构成的序列  A 满⾜：对于图中的每条边  (x,y) ， x 在  A 中都出现在  y 之前，则称  A 是该图的⼀
个拓扑序列。
且该序列必须满⾜下⾯两个条件：
1. 每个顶点出现且只出现⼀次。
2.  若存在⼀条从顶点  x 到顶点  y 的路径，那么在序列中顶点  x 出现在顶点  y 的前⾯。
拓扑排序只适⽤于  AOV ⽹  （有向⽆环图）
若图中有环，则⼀定不存在拓扑序。
可以证明，⼀个有向⽆环图，⼀定存在⼀个拓扑序列。有向⽆环图，⼜被称为拓扑图。
⼊度 : 即有多少条边指向⾃⼰这个节点。
出度 : 即有多少条边从⾃⼰这个节点指出去。
// break ⼗分关键，他保证了按照同⼀条线向下执⾏25
break;26

```
}
}
}
}
}
/**
```

* 拓扑排序算法2
*3
* @author wanghao4
*/5
public class Code3_TopologySort {6
public static List<Node> sortedTopology(Graph graph) {7
// key: 某⼀个 node8
// value: 剩余的⼊度9
HashMap<Node, Integer> inMap = new HashMap<>();10
// ⼊度为 0 的点，才能进这个队列11
Queue<Node> zeroInQueue = new LinkedList<>();12
for (Node node : graph.nodes.values()) {13
inMap.put(node, node.in);14
if (node.in == 0) {15
zeroInQueue.add(node);16

题⽬四：
kruskal 算法
适⽤范围：要求⽆向图
克鲁斯卡尔算法是求连通⽹的最⼩⽣成树的另⼀种⽅法。与不同，它的时间复杂度为 O （ eloge ）（ e 为
⽹中的边数），所以，适合于求边稀疏的⽹的最⼩⽣成树
最⼩⽣成树：
从最⼩的权重边开始加，如果产⽣环则不增加这个边（如何判断产⽣环）：

```
}
}
```

// 拓扑排序的结果，依次加⼊ result19
List<Node> result = new ArrayList<>();20
while (!zeroInQueue.isEmpty()) {21

```java
Node cur = zeroInQueue.poll();
result.add(cur);
for (Node next : cur.nexts) {
inMap.put(next, inMap.get(next) - 1);
if (inMap.get(next) == 0) {
zeroInQueue.add(next);
}
}
}
return result;
}
}
```

普⾥姆算法

/**1
* @author wanghao2
*  kruskal 算法原理3
* 现在我们假设⼀个图有 m 个节点， n 条边。⾸先，我们需要把 m 个节点看成 m 个独⽴的⽣成树，并且把 n 条边按
照从⼩到⼤的数据进⾏排列。在 n 条边中，我们依次取出其中的每⼀条边，如果发现边的两个节点分别位于两棵
树上，那么把两棵树合并成为⼀颗树；如果树的两个节点位于同⼀棵树上，那么忽略这条边，继续运⾏。等到所
有的边都遍历结束之后，如果所有的⽣成树可以合并成⼀条⽣成树，那么它就是我们需要寻找的最⼩⽣成树，反
之则没有最⼩⽣成树。
*/5
public class UnionFind {6
public HashMap<Node, List<Node>> setMap;7
public void makeSets(List<Node> nodes){9
for (Node cur: nodes){10
List<Node> set = new ArrayList<>();11

```sql
set.add(cur);
setMap.put(cur, set);
}
}
public boolean isSameSet(Node from, Node to){
List<Node> fromSet = setMap.get(from);
List<Node> toSet = setMap.get(to);
return fromSet==toSet;
}
```

Kruskal 克鲁斯卡尔算法：
public void union(Node from, Node to){22
List<Node> fromSet = setMap.get(from);23
List<Node> toSet = setMap.get(to);24
for (Node toNode: toSet){25
fromSet.add(toNode);26
setMap.put(toNode, fromSet);27

```
}
}
}
/**
```

* 权重的⽐较器2
* @author wanghao3
*/4
public class EdgeComparator implements Comparator<Edge> {5
@Override6
public int compare(Edge o1, Edge o2) {7
return o1.weight - o2.weight;8
}9

题⽬五：
prim 算法：（贪⼼）
适⽤范围：要求⽆向图
Prim 算法原理：
1 ）以某⼀个点开始，寻找当前该点可以访问的所有的边；
2 ）在已经寻找的边中发现最⼩边，这个边必须有⼀个点还没有访问过，将还没有访问的点加⼊我们的集合，记录添加
的边；
3 ）寻找当前集合可以访问的所有边，重复 2 的过程，直到没有新的点可以加⼊；
4 ）此时由所有边构成的树即为最⼩⽣成树。

```java
}
public class KruskalDemo {
public static Set<Edge> kruskalMST(Graph graph) {
UnionFind unionFind = new UnionFind();
unionFind.makeSets(new ArrayList<>(graph.nodes.values()));
PriorityQueue<Edge> priorityQueue = new PriorityQueue<>(new
EdgeComparator());
```

for (Edge edge : graph.edges) { // M 条边17
priorityQueue.add(edge); // O(logM)18

```java
}
Set<Edge> result = new HashSet<>();
```

while (!priorityQueue.isEmpty()) { // M 条边21

```sql
Edge edge = priorityQueue.poll(); // O(logM)
if (!unionFind.isSameSet(edge.from, edge.to)) { // O(1)
result.add(edge);
unionFind.union(edge.from, edge.to);
}
}
return result;
}
}
```

Prim_ 百度百科  (baidu.com)

public static Set<Edge> primMST(Graph graph) {1
// 解锁的边进⼊⼩根堆2
PriorityQueue<Edge> priorityQueue = new PriorityQueue<>(new

```java
EdgeComparator());
HashSet<Node> set = new HashSet<Node>();
```

总的来说
Prim 算法是以点为对象，挑选与点相连的最短边来构成最⼩⽣成树。⽽ Kruskal 算法是以边为对象，不断
地加⼊新的不构成环路的最短边来构成最⼩⽣成树。
题⽬六：
Dijkstra 算法
适⽤范围：没有权值为负的边
单元最短路径算法：

```java
Set<Edge> result = new HashSet<>();
```

for (Node node : graph.nodes.values()) { // 随便挑⼀个节点6
// node 是开始点7
if (!set.contains(node)) {8

```sql
set.add(node);
```

for (Edge edge : node.edges) { // 由⼀个节点，解锁所有相连的边10
priorityQueue.add(edge);11

```
}
while (!priorityQueue.isEmpty()) {
Edge edge = priorityQueue.poll(); // 弹出解锁的边中最⼩的边
Node toNode = edge.to; // 可能的⼀个新的节点
```

if (!set.contains(toNode)) { // 不包含的话就是新的节点16

```java
set.add(toNode);
result.add(edge);
for (Edge nextEdge : toNode.edges) {
priorityQueue.add(nextEdge);
}
}
}
}
break;
}
return result;
}
```

刚开始从 A 出发，除了 A ⾃⼰，其他距离都记做⽆穷⼤；更新后选最⼩距离的 B ，再从 B 出发，如果距离变的更短就更
新。
public static HashMap<Node, Integer> dijkstra1(Node head) {1
/**2

* 从 head 出发到所有点的最⼩距离3
* key ：从 head 出发到达 key4
* value ：从 head 出发到达 key 的最⼩距离5
* 如果在表中，没有 T 的记录，含义是从 head 出发到 T 这个点的距离是正⽆穷6
*/7
HashMap<Node, Integer> distanceMap = new HashMap<>();8
distanceMap.put(head, 0);9
// 已经求过距离的节点，存在 selectedNodes 中，以后再也不碰10
HashSet<Node> selectedNodes = new HashSet<>();11

```java
Node minNode = getMinDistanceAndUnseletedNode(distanceMap, selectedNodes);
while (minNode != null) {
int distance = distanceMap.get(minNode);
for (Edge edge : minNode.edges) {
Node toNode = edge.to;
if (!distanceMap.containsKey(toNode)) {
distanceMap.put(toNode, distance + edge.weight);
}
distanceMap.put(edge.to, Math.min(distanceMap.get(toNode), distance
+ edge.weight));
}
selectedNodes.add(minNode);
minNode = getMinDistanceAndUnseletedNode(distanceMap, selectedNodes);
}
return distanceMap;
}
private static Node getMinDistanceAndUnseletedNode(HashMap<Node, Integer>
distanceMap, HashSet<Node> selectedNodes) {
Node minNode = null;
int minDistance = Integer.MAX_VALUE;
for (Map.Entry<Node, Integer> entry : distanceMap.entrySet()) {
Node node = entry.getKey();
int distance = entry.getValue();
if (!selectedNodes.contains(node) && distance < minDistance) {
minNode = node;
minDistance = distance;
}
}
return minNode;
}
public static class NodeRecord {
public Node node;
```

利⽤⼩根堆来优化这个过程：
需要⾃⼰修改⼩根堆的操作，系统默认提供的⼩根堆⽆法实现修改完某⼀个位置的值之后继续维持⼩根堆结构，系统默
认功能只从堆顶去掉⼀个元素之后其他的值继续保持⼩根堆。
public int distance;44
public NodeRecord(Node node, int distance) {46

```yaml
this.node = node;
this.distance = distance;
}
}
/**
```

* 利⽤⼩根堆优化后的流程2
* 需⾃定义⼩根堆的操作 : 修改某个位置后仍然保持⼩根堆结构3
*4
* 从 head 出发，所有 head 能够到达的节点，⽣成到达每个节点的最⼩路径记录并返回5
*/6
public static class NodeRecord {7
public Node node;8
public int distance;9
public NodeRecord(Node node, int distance) {11

```java
this.node = node;
this.distance = distance;
}
}
public static HashMap<Node, Integer> dijkstra2(Node head, int size) {
NodeHeap nodeHeap = new NodeHeap(size);
nodeHeap.addOrUpdateOrIgnore(head, 0);
HashMap<Node, Integer> result = new HashMap<>();
while (!nodeHeap.isEmpty()) {
NodeRecord record = nodeHeap.pop();
Node cur = record.node;
int distance = record.distance;
for (Edge edge : cur.edges) {
nodeHeap.addOrUpdateOrIgnore(edge.to, edge.weight + distance);
}
result.put(cur, distance);
```

```java
}
return result;
}
public static class NodeHeap {
private Node[] nodes;
private HashMap<Node, Integer> heapIndexMap;
private HashMap<Node, Integer> distanceMap;
private int size;
public NodeHeap(int size) {
nodes = new Node[size];
heapIndexMap = new HashMap<>();
distanceMap = new HashMap<>();
this.size = 0;
}
public boolean isEmpty() {
return size == 0;
}
public void addOrUpdateOrIgnore(Node node, int distance) {
if (inHeap(node)) {
distanceMap.put(node, Math.min(distanceMap.get(node), distance));
insertHeapify(node, heapIndexMap.get(node));
}
if (!isEntered(node)) {
nodes[size] = node;
heapIndexMap.put(node, size);
distanceMap.put(node, distance);
insertHeapify(node, size++);
}
}
public NodeRecord pop() {
NodeRecord nodeRecord = new NodeRecord(nodes[0],
distanceMap.get(nodes[0]));
swap(0, size - 1);
heapIndexMap.put(nodes[size - 1], -1);
distanceMap.remove(nodes[size - 1]);
nodes[size - 1] = null;
heapify(0, --size);
```

return nodeRecord;70

```java
}
private void insertHeapify(Node node, int index) {
while (distanceMap.get(nodes[index]) < distanceMap.get(nodes[(index - 1)
/ 2])) {
swap(index, (index - 1) / 2);
index = (index - 1) / 2;
}
}
private void heapify(int index, int size) {
int left = index * 2 + 1;
while (left < size) {
int smallest = left + 1 < size && distanceMap.get(nodes[left + 1]) <
distanceMap.get(nodes[left])
? left + 1 : left;
smallest = distanceMap.get(nodes[smallest]) <
distanceMap.get(nodes[index]) ? smallest : index;
if (smallest == index) {
break;
}
swap(smallest, index);
index = smallest;
left = index * 2 + 1;
}
}
private boolean isEntered(Node node) {
return heapIndexMap.containsKey(node);
}
private boolean inHeap(Node node) {
return isEntered(node) && heapIndexMap.get(node) != -1;
}
private void swap(int index1, int index2) {
heapIndexMap.put(nodes[index1], index2);
heapIndexMap.put(nodes[index2], index1);
Node tmp = nodes[index1];
nodes[index1] = nodes[index2];
nodes[index2] = tmp;
}
```

题⽬九：
N 皇后问题是指在 N*N 的棋盘上要摆 N 个皇后，要求任何两个皇后不同⾏，不同列，也不在同⼀条斜线上。
给定⼀个整数 n ，返回 n 皇后的摆法有多少种。

```java
n=1, 返回
n=2 或 3 ， 2 皇后和 3 皇后问题⽆论怎么摆都不⾏，返回
n=8 ，返回
}
public static int num1(int n) {
if (n < 1) {
return 0;
}
```

int[] record = new int[n]; // record[i] -> i ⾏的皇后，放在了第⼏列5
return process1(0, record, n);6

```
}
/**
```

* 潜台词： record[0...i-1] 的皇后，任何两个皇后⼀定都不同⾏，不同列，不公斜线9
* ⽬前来到了第 i ⾏10
* record[0...i-1] 表示之前的⾏，放了皇后的位置11
* n 代表整体⼀共有多少⾏12
* 返回值是，摆完所有的皇后，合理的摆法有多少种13
*/14

利⽤位运算进⾏优化 (y1s1 有点没看懂 ) ：
private static int process1(int i, int[] record, int n) {15
if (i == n) { // 终⽌⾏16
return 1;17

```
}
int res = 0;
```

for (int j = 0; j < n; j++) { // 当前⾏在 i ⾏，尝试 i ⾏所有的列  -> j20
// 当前 i ⾏的皇后，放在 j 列，会不会和之前 (0...i-1) 的皇后，不同⾏同列或者同斜线21
// 如果是，认为有效22
// 如果不是，认为⽆效23
if (isValid(record, i, j)) {24

```java
record[i] = j;
res += process1(i + 1, record, n);
}
}
return res;
}
/**
```

* record[0...i-1] 你需要看， record[i...] 不需要看33
* 返回 i ⾏皇后，放在了 j 列，是否有效34
*/35
private static boolean isValid(int[] record, int i, int j) {36
for (int k = 0; k < i; k++) {37
if (j == record[k] || Math.abs(record[k] - j) == Math.abs(i - k)) {38
return false;39

```java
}
}
return true;
}
/**
```

* 利⽤位运算来优化2
* 不要超过 32 皇后问题3
*/4
public static int num2(int n) {5
if (n < 1 || n > 32) {6
return 0;7
}8

```java
int limit = n == 32 ? -1 : (1 << n) - 1;
return process2(limit, 0, 0, 0);
}
/**
```

* colLim 列的限制， 1 的位置不能放皇后， 0 的位置可以14
* leftDiaLim 左斜线的限制， 1 的位置不能放皇后， 0 的位置可以15
* rightDiaLim 右斜线的限制， 1 的位置不能放皇后， 0 的位置可以16
*/17
private static int process2(int limit, int colLim, int leftDiaLim,18
int rightDiaLim) {19
if (colLim == limit) { // base case20
return 1;21

```java
}
int pos = 0;
int mostRightOne = 0;
pos = limit & (~(colLim | leftDiaLim | rightDiaLim));
int res = 0;
while (pos != 0) {
mostRightOne = pos & (~pos + 1);
pos = pos - mostRightOne;
res += process2(limit, colLim | mostRightOne,
(leftDiaLim | mostRightOne) << 1,
(rightDiaLim | mostRightOne) >>> 1);
}
return res;
}
```
