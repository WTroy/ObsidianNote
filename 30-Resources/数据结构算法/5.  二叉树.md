---
title: 5.  二叉树
source: 有道云笔记
imported: true
---

⼆叉树节点结构：
⽤递归和⾮递归两种⽅式实现⼆叉树的先序，中序，后序遍历。
如何直观的打印⼀棵⼆叉树？
如何完成⼆叉树的宽度优先遍历（常⻅题⽬：求⼀颗⼆叉树的宽度）？
递归序：
三种顺序：
先序：对于所有⼦树，先打印头节点  然后左  然后右；第⼀次遇⻅节点打印
中序：对于所有⼦树，先打印左节点  然后头  然后右；第⼆次遇⻅节点打印
后序：对于所有⼦树，先打印左节点  然后右  然后头；第三次遇⻅节点打印
class Node<V>{1
V value;2
Node left;3
Node right;4
}5
public static void f(Node root){1
// 12
if (root == null){3
return;4
}5
// 16
f(root.left);7
// 28
// 29
f(root.right);10
// 311
// 312
}13

/**1
* 递归实现前序遍历2
*/3
public static void preOrderRecur(Node head){4
if (head==null){5
return;6
}7
System.out.println(head.value+" ");8
preOrderRecur(head.left);9
preOrderRecur(head.right);10
}11
12
/**13
* 递归实现中序遍历14
*/15
public static void inOrderRecur(Node head){16
if (head==null){17
return;18
}19
preOrderRecur(head.left);20
System.out.println(head.value+" ");21
preOrderRecur(head.right);22
}23
24
/**25

⾮递归的实现（任何递归都可以改为⾮递归）：
先序：
* 递归实现后序遍历26
*/27
public static void posOrderRecur(Node head){28
if (head==null){29
return;30
}31
preOrderRecur(head.left);32
preOrderRecur(head.right);33
System.out.println(head.value+" ");34
}35
/**1
* ⾮递归的先序遍历⼆叉树2
* 1 从栈中弹出⼀个节点 cur3
* 2 打印（处理） cur4
* 3 先右后左进栈（如果有）5
* 4 回到第⼀步6
* @param head7
*/8

后序遍历：
public static void preOrderUnRecur(Node head) {9
System.out.println("pre-order: ");10
if (head != null) {11
Stack<Node> stack = new Stack<>();12
stack.add(head);13
while (!stack.isEmpty()) {14
head = stack.pop();15
System.out.println(head.value + " ");16
if (head.right != null) {17
stack.push(head.right);18
}19
if (head.left != null) {20
stack.push(head.left);21
}22
}23
}24
System.out.println();25
}26
public static void posOrderUnRecur(Node head) {1
System.out.println("pos-order: ");2
if (head != null) {3

中序遍历：
Stack<Node> s1 = new Stack<>();4
Stack<Node> s2 = new Stack<>();5
s1.push(head);6
while (!s1.isEmpty()) {7
head = s1.pop();8
s2.push(head);9
if (head.left != null) {10
s1.push(head.left);11
}12
if (head.right != null) {13
s1.push(head.right);14
}15
}16
while (!s2.isEmpty()) {17
System.out.println(s2.pop().value + " ");18
}19
}20
System.out.println();21
}22
/**1

如何完成⼆叉树的宽度优先遍历（常⻅题⽬：求⼀棵⼆叉树的宽度）
* 中序遍历2
* 每颗⼦树⽽⾔：整棵树左边界进栈，依次弹出的过程中打印，并对弹出节点的右⼦树进⾏同样的操作3
* @param head4
*/5
public static void inOrderUnRecur(Node head) {6
System.out.println("in-order: ");7
if (head != null) {8
Stack<Node> stack = new Stack<>();9
while (!stack.isEmpty() || head != null) {10
if (head != null) {11
stack.push(head);12
head = head.left;13
} else {14
head = stack.pop();15
System.out.println(head.value + " ");16
head = head.right;17
}18
}19
}20
System.out.println();21
}22

宽度优先遍历：
Hash 的实现：
/**1
* 利⽤队列来实现宽度优先遍历的实现基础2
* @param head3
*/4
public static void w(Node head) {5
if (head == null) {6
return;7
}8
Queue<Node> queue = new LinkedList<>();9
queue.add(head);10
while (!queue.isEmpty()) {11
Node cur = queue.poll();12
System.out.println(cur.value);13
if (cur.left != null) {14
queue.add(cur.left);15
}16
if (cur.right != null) {17
queue.add(cur.right);18
}19
}20
}21
/**1
* 在上述遍历的过程中，利⽤ hash 表来实现宽度优先遍历2
* 3
* @param head4
* @return5
*/6
public static int wHash(Node head) {7
Queue<Node> queue = new LinkedList<>();8
queue.add(head);9
HashMap<Node, Integer> levelMap = new HashMap<>();10
levelMap.put(head, 1);11
int curLevel = 1;12
int curLevelNodes = 0;13

⾮ hash 表：
依赖队列，
三个变量：当前层的最后⼀个节点；下⼀层的最后⼀个节点；当前层的节点数
int max = Integer.MIN_VALUE;14
while (!queue.isEmpty()) {15
Node cur = queue.poll();16
int curNodeLevel = levelMap.get(cur);17
if (curNodeLevel == curLevel) {18
curLevelNodes++;19
} else {20
max = Math.max(max, curLevelNodes);21
curLevel++;22
curLevelNodes = 1;23
}24
if (cur.left != null) {25
levelMap.put(cur.left, curNodeLevel + 1);26
queue.add(cur.left);27
}28
if (cur.right != null) {29
levelMap.put(cur.right, curNodeLevel + 1);30
queue.add(cur.right);31
}32
}33
return max;34
}35

/**1
* 只利⽤队列来实现宽度优先遍历并返回最⼤宽度2
* @param head3
* @return4
*/5
public static int qHash(Node head) {6
if (head == null) {7
return 0;8
}9
Queue<Node> queue = new LinkedList<>();10
// 最⼤寬度11
int maxWidth = Integer.MIN_VALUE;12
// ⼊队13
queue.add(head);14
while (true) {15
// 当前层的节点个数16
int len = queue.size();17
if (len == 0) {18
break;19
}20
// 当前层还有节点21
while (len > 0) {22

题⽬⼆：
如何判断⼀颗⼆叉树是否是搜索⼆叉树？
如何判断⼀颗⼆叉树是完全⼆叉树？
如何判断⼀颗⼆叉树是否是满⼆叉树？
如何判断⼀颗⼆叉树是否是平衡⼆叉树？（⼆叉树题⽬套路）
判断是否搜索⼆叉树：
（ Binary Search Tree ），（⼜：⼆叉搜索树，⼆叉排序树）它或者是⼀棵空树，或者是具有下列性质
的 ：  若它的左⼦树不空，则左⼦树上所有结点的值均⼩于它的的值；  若它的右⼦树不空，则右⼦树
上所有结点的值均⼤于它的根结点的值；  它的左、右⼦树也分别为 。⼆叉搜索树作为⼀种经典的数据
结构，它既有链表的快速插⼊与删除操作的特点，⼜有数组快速查找的优势；所以应⽤⼗分⼴泛，例如在⽂件系统
和数据库系统⼀般会采⽤这种数据结构进⾏⾼效率的排序与检索操作
中序遍历，结果是升序则是不然就不是。
// 在队列元素为空的情况下， remove() ⽅法会抛出 NoSuchElementException 异常，
poll() ⽅法只会返回  null 。
23
// 从队列头部删除⼀个元素。24
Node t = queue.poll();25
len--;26
if (t.left != null) {27
// 下⼀层左节点⼊队28
queue.add(t.left);29
}30
if (t.right != null) {31
// 下⼀层右节点⼊队32
queue.add(t.right);33
}34
}35
maxWidth = Math.max(maxWidth, queue.size());36
}37
return maxWidth;38
}39
⼆叉查找树
⼆叉树 根结点
⼆叉排序树
/**1
* 判断是否搜索⼆叉树：2
* 中序遍历，结果是升序则是不然就不是3
*/4
public static int preValue = Integer.MIN_VALUE;5
public static boolean checkBST(Node<Integer> head){6
if (head == null){7
return true;8
}9

完全⼆叉树：
boolean isLeftBst = checkBST(head.left);10
if (!isLeftBst){11
return false;12
}13
if (head.value <= preValue){14
return false;15
}else {16
preValue=head.value;17
}18
return checkBST(head.right);19
}20
21
/**22
* 通过栈来判断是否搜索⼆叉树23
* @param head24
* @return25
*/26
public static boolean checkBST3(Node<Integer> head){27
if (head !=null){28
int preValue = Integer.MIN_VALUE;29
Stack<Node> stack = new Stack<>();30
while (!stack.isEmpty() || head!=null){31
if (head!=null){32
stack.push(head);33
head = head.left;34
}else {35
head = stack.pop();36
if (head.value<=preValue){37
return false;38
}else {39
preValue = head.value;40
}41
head = head.right;42
}43
}44
}45
return true;46
}47

/**1
* 完全⼆叉树的判断规则：2
* 1 任意节点有有孩⼦⽽没有左孩⼦  则返回 false3
* 2 在不违反规则 1 的前提下，如果遇到了第⼀个左右孩⼦不全，则后序节点都是叶⼦节点4
* @param head5
* @return6
*/7
public static boolean isCBT(Node head) {8
if (head == null) {9
return true;10
}11
LinkedList<Node> queue = new LinkedList<>();12
// 是否遇到过左右两个孩⼦不双全的节点13
boolean leaf = false;14
Node l = null;15
Node r = null;16
queue.add(head);17
while (!queue.isEmpty()) {18
head = queue.poll();19
l = head.left;20
r = head.right;21
if ((leaf && (l != null || r != null)) || (l == null && r != null)) {22
return false;23

满⼆叉树：
是否平衡⼆叉树（递归套路的思考⽅式）：
此处平衡⼆叉树是指每个节点的左右⼦树⾼度差不能超过 1
应该避免和平衡⼆叉搜索树混淆概念：此处并没有左⼦树节点⼩于头节点，右⼦树节点⼤于头节点的要求
}24
if (l != null) {25
queue.add(l);26
}27
if (r != null) {28
queue.add(r);29
}30
if (l == null || r == null) {31
leaf = true;32
}33
}34
return true;35
}36

/**1
* 判断是否是平衡⼆叉树2
* 注意和平衡⼆叉搜索树的区别3
* @param head4
* @return5
*/6
public static boolean isBalanced(Node head){7
return process(head).isBalanced;8
}9
private static ReturnType process(Node x) {10
if (x==null){11
return new ReturnType(true, 0);12
}13
ReturnType leftData = process(x.left);14
ReturnType rightData = process(x.right);15
int height = Math.max(leftData.height, rightData.height) + 1;16
boolean isBalanced = leftData.isBalanced && rightData.isBalanced &&
Math.abs(leftData.height-rightData.height)<2;
17
return new ReturnType(isBalanced, height);18
}19
public static class ReturnType{20
private boolean isBalanced;21
private int height;22
public ReturnType(boolean isBalanced, int height) {23
this.isBalanced = isBalanced;24
this.height = height;25

搜索⼆叉树的思索⽅式：
}26
}27
/**1
* 搜索⼆叉树的判断2
* 没有平衡这个属性  所以不要于 AVL 混合3
*/4
public static class ReturnDataBTS {5
public boolean isBST;6
public int min;7
public int max;8
9
public ReturnDataBTS(boolean isBST, int min, int max) {10
this.isBST = isBST;11
this.min = min;12
this.max = max;13
}14
}15

树形 DP ：在树上进⾏动态规划
满⼆叉树：
public static ReturnDataBTS processBTS(Node<Integer> x) {16
if (x == null) {17
return null;18
}19
ReturnDataBTS leftData = processBTS(x.left);20
ReturnDataBTS rightData = processBTS(x.right);21
int min = x.value;22
int max = x.value;23
if (leftData != null) {24
min = Math.min(min, leftData.min);25
max = Math.max(max, leftData.max);26
}27
if (rightData != null) {28
min = Math.min(min, rightData.min);29
max = Math.max(max, rightData.max);30
}31
boolean isBST = true;32
if (leftData != null && (!leftData.isBST || leftData.max > x.value)) {33
isBST = false;34
}35
if (rightData != null && (!rightData.isBST || rightData.min < x.value)) {36
isBST = false;37
}38
return new ReturnDataBTS(isBST, min, max);39
}40
/**1
* 是否是满⼆叉树2
*3
*/4
public static boolean isF(Node head) {5
if (head == null) {6
return true;7
}8
Info data = f(head);9
return data.nodes == (1 << data.height - 1);10

题⽬⼆：
给定两个⼆叉树的节点 node1 和 node2 ，找到他们的最低公共祖先节点？
o1 o2 肯定在 head ⾥：
}11
private static Info f(Node x) {12
if (x == null) {13
return new Info(0, 0);14
}15
Info leftData = f(x.left);16
Info rightData = f(x.right);17
int height = Math.max(leftData.height, rightData.height) + 1;18
int nodes = leftData.nodes + rightData.nodes + 1;19
return new Info(height, nodes);20
}21
public static class Info {22
public int height;23
public int nodes;24
25
public Info(int height, int nodes) {26
this.height = height;27
this.nodes = nodes;28
}29
}30
/**1
* 获取两个节点 o1 o2 的最低共同⽗节点2
* 3
*/4
public static Node lca(Node head, Node o1, Node o2) {5
HashMap<Node, Node> fatherMap = new HashMap<>();6
fatherMap.put(head, head);7
process(head, fatherMap);8
HashSet<Node> set1 = new HashSet<>();9
Node cur = o1;10
while (cur != fatherMap.get(cur)) {11
set1.add(cur);12
cur = fatherMap.get(cur);13
}14
set1.add(head);15

或者：
Node cur2 = o2;16
while (cur2!= fatherMap.get(cur2)){17
if (set1.contains(cur2)){18
return cur2;19
}20
cur2 = fatherMap.get(cur2);21
}22
return null;23
}24
private static void process(Node head, HashMap<Node, Node> fatherMap) {25
if (head == null) {26
return;27
}28
fatherMap.put(head.left, head);29
fatherMap.put(head.right, head);30
process(head.left, fatherMap);31
process(head.right, fatherMap);32
}33
/**1
* 递归获取两个节点 o1 o2 的最低共同⽗节点2
*/3
public static Node lowestAncestor(Node head, Node o1, Node o2) {4
// base case5
if (head == null || head == o1 || head == o2) {6
return head;7
}8
Node left = lowestAncestor(head.left, o1, o2);9
Node right = lowestAncestor(head.right, o1, o2);10
if (right != null && right != null) {11
return head;12
}13
// 左右两棵树  并不是都有返回值14
return left != null ? left : right;15
}16

题⽬四：
在⼆叉树中找到⼀个节点的后继节点。
题⽬：现有⼀种新的⼆叉树节点类型如下：
public class NodeWithParent {1
public int value;2

该结构⽐普通⼆叉树节点结构多了⼀个指向⽗节点的 parent 指针。
假设有⼀个这个节点类型组成的⼆叉树，树中每个节点的 parent 指针都正确的指向⾃⼰的⽗节点，头节点的 parent 指向
null 。
只给⼀个在⼆叉树中的某个节点 node ，请实现返回 node 的后继节点的函数。
在⼆叉树的中序遍历的序列中， node 的下⼀个节点叫做 node 的后续节点。
public NodeWithParent left;3
public NodeWithParent right;4
public NodeWithParent parent;5
6
public NodeWithParent(int value) {7
this.value = value;8
}9
}10
public static NodeWithParent getSuccessorNode(NodeWithParent node){1
if (node == null){2
return node;3
}4
if (node.right!=null){5
return getLeftMost(node.right);6
}else {7

题⽬五：
⼆叉树的序列化和反序列化
就是内存⾥的⼀棵树如何变成字符串形式，⼜如何从字符串变成内存⾥的树。
如何判断⼀棵⼆叉树是不是另⼀棵⼆叉树的⼦树？
先序序列化举例：
// ⽆右⼦树8
NodeWithParent parent = node.parent;9
// 当前节点是其⽗亲节点右孩⼦10
while (parent!=null && parent.left!=node){11
node = parent;12
parent = node.parent;13
}14
return parent;15
}16
}17
18
private static NodeWithParent getLeftMost(NodeWithParent node) {19
if (node == null){20
return node;21
}22
while (node.left != null){23
node = node.left;24
}25
return node;26
}27

/**1
* 以 head 为头的树，请序列化成字符串返回2
*/3
public static String serialPre(Node head) {4
if (head == null) {5
return "#_";6
}7
String res = head.value + "_";8
res += serialPre(head.left);9
res += serialPre(head.right);10
return res;11
}12
13
public static Node reconByPreString(String preStr) {14
String[] values = preStr.split("_");15
Queue<String> queue = new LinkedList<>();16
for (int i = 0; i != values.length; i++) {17
queue.add(values[i]);18
}19
return reconPreOrder(queue);20
}21
22
private static Node reconPreOrder(Queue<String> queue) {23

对折纸条，打印所有折痕的⽅向（⼆叉树的中序遍历）：
String value = queue.poll();24
if (value.equals("#")) {25
return null;26
}27
Node head = new Node(Integer.valueOf(value));28
head.left = reconPreOrder(queue);29
head.right = reconPreOrder(queue);30
return head;31
}32
/**1
* 以下是折纸问题：2
* 对折纸条，打印所有折痕的⽅向，这其实就是⼀个⼆叉树的中序遍历3
* @param N4
*/5
public static void printAllFolds(int N) {6
printProcess(1, N, true);7
}8
// 递归过程，来到了某⼀个节点9

// i 是节点的层数， n ⼀共的层数， down=true 凹  down=false 凸10
private static void printProcess(int i, int n, boolean down) {11
if (i > n) {12
return;13
}14
printProcess(i + 1, n, true);15
System.out.println(down ? " 凹 " : " 凸 ");16
printProcess(i + 1, n, false);17
}18
19
public static void main(String[] args) {20
int N = 3;21
printAllFolds(N);22
}23