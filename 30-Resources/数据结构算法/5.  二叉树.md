---
title: 5.  二叉树
source: 有道云笔记
imported: true
---

⼆叉树节点结构：
⽤递归和⾮递归两种⽅式实现⼆叉树的先序，中序，后序遍历。
如何直观的打印⼀棵⼆叉树？
如何完成⼆叉树的宽度优先遍历（常⻅题⽬：求⼀颗⼆叉树的宽度）？
递归序：
三种顺序：
先序：对于所有⼦树，先打印头节点  然后左  然后右；第⼀次遇⻅节点打印
中序：对于所有⼦树，先打印左节点  然后头  然后右；第⼆次遇⻅节点打印
后序：对于所有⼦树，先打印左节点  然后右  然后头；第三次遇⻅节点打印
class Node<V>{1
V value;2
Node left;3
Node right;4

```java
}
public static void f(Node root){
//
if (root == null){
return;
}
//
f(root.left);
//
//
f(root.right);
// 311
// 312
}
```

/**1
* 递归实现前序遍历2
*/3
public static void preOrderRecur(Node head){4
if (head==null){5
return;6

```
}
System.out.println(head.value+" ");
preOrderRecur(head.left);
preOrderRecur(head.right);
}
/**
```

* 递归实现中序遍历14
*/15
public static void inOrderRecur(Node head){16
if (head==null){17
return;18

```
}
preOrderRecur(head.left);
System.out.println(head.value+" ");
preOrderRecur(head.right);
}
/**
```

⾮递归的实现（任何递归都可以改为⾮递归）：
先序：
* 递归实现后序遍历26
*/27
public static void posOrderRecur(Node head){28
if (head==null){29
return;30

```
}
preOrderRecur(head.left);
preOrderRecur(head.right);
System.out.println(head.value+" ");
}
/**
```

* ⾮递归的先序遍历⼆叉树2
* 1 从栈中弹出⼀个节点 cur3
* 2 打印（处理） cur4
* 3 先右后左进栈（如果有）5
* 4 回到第⼀步6
* @param head7
*/8

后序遍历：
public static void preOrderUnRecur(Node head) {9
System.out.println("pre-order: ");10
if (head != null) {11
Stack<Node> stack = new Stack<>();12
stack.add(head);13
while (!stack.isEmpty()) {14

```java
head = stack.pop();
System.out.println(head.value + " ");
if (head.right != null) {
stack.push(head.right);
}
if (head.left != null) {
stack.push(head.left);
}
}
}
System.out.println();
}
public static void posOrderUnRecur(Node head) {
System.out.println("pos-order: ");
if (head != null) {
```

中序遍历：
Stack<Node> s1 = new Stack<>();4
Stack<Node> s2 = new Stack<>();5
s1.push(head);6
while (!s1.isEmpty()) {7

```yaml
head = s1.pop();
s2.push(head);
if (head.left != null) {
s1.push(head.left);
}
if (head.right != null) {
s1.push(head.right);
}
}
while (!s2.isEmpty()) {
System.out.println(s2.pop().value + " ");
}
}
System.out.println();
}
/**
```

如何完成⼆叉树的宽度优先遍历（常⻅题⽬：求⼀棵⼆叉树的宽度）
* 中序遍历2
* 每颗⼦树⽽⾔：整棵树左边界进栈，依次弹出的过程中打印，并对弹出节点的右⼦树进⾏同样的操作3
* @param head4
*/5
public static void inOrderUnRecur(Node head) {6
System.out.println("in-order: ");7
if (head != null) {8
Stack<Node> stack = new Stack<>();9
while (!stack.isEmpty() || head != null) {10
if (head != null) {11
stack.push(head);12

```yaml
head = head.left;
} else {
head = stack.pop();
System.out.println(head.value + " ");
head = head.right;
}
}
}
System.out.println();
}
```

宽度优先遍历：
Hash 的实现：
/**1
* 利⽤队列来实现宽度优先遍历的实现基础2
* @param head3
*/4
public static void w(Node head) {5
if (head == null) {6
return;7

```java
}
Queue<Node> queue = new LinkedList<>();
queue.add(head);
while (!queue.isEmpty()) {
Node cur = queue.poll();
System.out.println(cur.value);
if (cur.left != null) {
queue.add(cur.left);
}
if (cur.right != null) {
queue.add(cur.right);
}
}
}
/**
```

* 在上述遍历的过程中，利⽤ hash 表来实现宽度优先遍历2
* 3
* @param head4
* @return5
*/6
public static int wHash(Node head) {7
Queue<Node> queue = new LinkedList<>();8
queue.add(head);9
HashMap<Node, Integer> levelMap = new HashMap<>();10
levelMap.put(head, 1);11

```
int curLevel = 1;
int curLevelNodes = 0;
```

⾮ hash 表：
依赖队列，
三个变量：当前层的最后⼀个节点；下⼀层的最后⼀个节点；当前层的节点数

```java
int max = Integer.MIN_VALUE;
while (!queue.isEmpty()) {
Node cur = queue.poll();
int curNodeLevel = levelMap.get(cur);
if (curNodeLevel == curLevel) {
curLevelNodes++;
} else {
max = Math.max(max, curLevelNodes);
curLevel++;
curLevelNodes = 1;
}
if (cur.left != null) {
levelMap.put(cur.left, curNodeLevel + 1);
queue.add(cur.left);
}
if (cur.right != null) {
levelMap.put(cur.right, curNodeLevel + 1);
queue.add(cur.right);
}
}
return max;
}
```

/**1
* 只利⽤队列来实现宽度优先遍历并返回最⼤宽度2
* @param head3
* @return4
*/5
public static int qHash(Node head) {6
if (head == null) {7
return 0;8

```java
}
Queue<Node> queue = new LinkedList<>();
```

// 最⼤寬度11

```
int maxWidth = Integer.MIN_VALUE;
```

// ⼊队13
queue.add(head);14
while (true) {15
// 当前层的节点个数16

```
int len = queue.size();
if (len == 0) {
break;
}
```

// 当前层还有节点21
while (len > 0) {22

题⽬⼆：
如何判断⼀颗⼆叉树是否是搜索⼆叉树？
如何判断⼀颗⼆叉树是完全⼆叉树？
如何判断⼀颗⼆叉树是否是满⼆叉树？
如何判断⼀颗⼆叉树是否是平衡⼆叉树？（⼆叉树题⽬套路）
判断是否搜索⼆叉树：
（ Binary Search Tree ），（⼜：⼆叉搜索树，⼆叉排序树）它或者是⼀棵空树，或者是具有下列性质
的 ：  若它的左⼦树不空，则左⼦树上所有结点的值均⼩于它的的值；  若它的右⼦树不空，则右⼦树
上所有结点的值均⼤于它的根结点的值；  它的左、右⼦树也分别为 。⼆叉搜索树作为⼀种经典的数据
结构，它既有链表的快速插⼊与删除操作的特点，⼜有数组快速查找的优势；所以应⽤⼗分⼴泛，例如在⽂件系统
和数据库系统⼀般会采⽤这种数据结构进⾏⾼效率的排序与检索操作
中序遍历，结果是升序则是不然就不是。
// 在队列元素为空的情况下， remove() ⽅法会抛出 NoSuchElementException 异常，
poll() ⽅法只会返回  null 。
// 从队列头部删除⼀个元素。24

```
Node t = queue.poll();
len--;
if (t.left != null) {
```

// 下⼀层左节点⼊队28
queue.add(t.left);29

```
}
if (t.right != null) {
```

// 下⼀层右节点⼊队32
queue.add(t.right);33

```java
}
}
maxWidth = Math.max(maxWidth, queue.size());
}
return maxWidth;
}
```

⼆叉查找树
⼆叉树 根结点
⼆叉排序树
/**1
* 判断是否搜索⼆叉树：2
* 中序遍历，结果是升序则是不然就不是3
*/4

```java
public static int preValue = Integer.MIN_VALUE;
public static boolean checkBST(Node<Integer> head){
if (head == null){
return true;
}
```

完全⼆叉树：

```java
boolean isLeftBst = checkBST(head.left);
if (!isLeftBst){
return false;
}
if (head.value <= preValue){
return false;
}else {
preValue=head.value;
}
return checkBST(head.right);
}
/**
```

* 通过栈来判断是否搜索⼆叉树23
* @param head24
* @return25
*/26
public static boolean checkBST3(Node<Integer> head){27
if (head !=null){28

```java
int preValue = Integer.MIN_VALUE;
Stack<Node> stack = new Stack<>();
while (!stack.isEmpty() || head!=null){
if (head!=null){
stack.push(head);
head = head.left;
}else {
head = stack.pop();
if (head.value<=preValue){
return false;
}else {
preValue = head.value;
}
head = head.right;
}
}
}
return true;
}
```

/**1
* 完全⼆叉树的判断规则：2
* 1 任意节点有有孩⼦⽽没有左孩⼦  则返回 false3
* 2 在不违反规则 1 的前提下，如果遇到了第⼀个左右孩⼦不全，则后序节点都是叶⼦节点4
* @param head5
* @return6
*/7
public static boolean isCBT(Node head) {8
if (head == null) {9
return true;10

```java
}
LinkedList<Node> queue = new LinkedList<>();
```

// 是否遇到过左右两个孩⼦不双全的节点13

```java
boolean leaf = false;
Node l = null;
Node r = null;
queue.add(head);
while (!queue.isEmpty()) {
head = queue.poll();
l = head.left;
r = head.right;
if ((leaf && (l != null || r != null)) || (l == null && r != null)) {
return false;
```

满⼆叉树：
是否平衡⼆叉树（递归套路的思考⽅式）：
此处平衡⼆叉树是指每个节点的左右⼦树⾼度差不能超过 1
应该避免和平衡⼆叉搜索树混淆概念：此处并没有左⼦树节点⼩于头节点，右⼦树节点⼤于头节点的要求

```java
}
if (l != null) {
queue.add(l);
}
if (r != null) {
queue.add(r);
}
if (l == null || r == null) {
leaf = true;
}
}
return true;
}
```

/**1
* 判断是否是平衡⼆叉树2
* 注意和平衡⼆叉搜索树的区别3
* @param head4
* @return5
*/6
public static boolean isBalanced(Node head){7
return process(head).isBalanced;8

```java
}
private static ReturnType process(Node x) {
if (x==null){
return new ReturnType(true, 0);
}
ReturnType leftData = process(x.left);
ReturnType rightData = process(x.right);
int height = Math.max(leftData.height, rightData.height) + 1;
boolean isBalanced = leftData.isBalanced && rightData.isBalanced &&
Math.abs(leftData.height-rightData.height)<2;
return new ReturnType(isBalanced, height);
}
public static class ReturnType{
private boolean isBalanced;
private int height;
public ReturnType(boolean isBalanced, int height) {
this.isBalanced = isBalanced;
this.height = height;
```

搜索⼆叉树的思索⽅式：

```
}
}
/**
```

* 搜索⼆叉树的判断2
* 没有平衡这个属性  所以不要于 AVL 混合3
*/4
public static class ReturnDataBTS {5
public boolean isBST;6
public int min;7
public int max;8
public ReturnDataBTS(boolean isBST, int min, int max) {10

```yaml
this.isBST = isBST;
this.min = min;
this.max = max;
}
}
```

树形 DP ：在树上进⾏动态规划
满⼆叉树：
public static ReturnDataBTS processBTS(Node<Integer> x) {16
if (x == null) {17
return null;18

```java
}
ReturnDataBTS leftData = processBTS(x.left);
ReturnDataBTS rightData = processBTS(x.right);
int min = x.value;
int max = x.value;
if (leftData != null) {
min = Math.min(min, leftData.min);
max = Math.max(max, leftData.max);
}
if (rightData != null) {
min = Math.min(min, rightData.min);
max = Math.max(max, rightData.max);
}
boolean isBST = true;
if (leftData != null && (!leftData.isBST || leftData.max > x.value)) {
isBST = false;
}
if (rightData != null && (!rightData.isBST || rightData.min < x.value)) {
isBST = false;
}
return new ReturnDataBTS(isBST, min, max);
}
/**
```

* 是否是满⼆叉树2
*3
*/4
public static boolean isF(Node head) {5
if (head == null) {6
return true;7

```java
}
Info data = f(head);
return data.nodes == (1 << data.height - 1);
```

题⽬⼆：
给定两个⼆叉树的节点 node1 和 node2 ，找到他们的最低公共祖先节点？
o1 o2 肯定在 head ⾥：

```java
}
private static Info f(Node x) {
if (x == null) {
return new Info(0, 0);
}
Info leftData = f(x.left);
Info rightData = f(x.right);
int height = Math.max(leftData.height, rightData.height) + 1;
int nodes = leftData.nodes + rightData.nodes + 1;
return new Info(height, nodes);
}
public static class Info {
public int height;
public int nodes;
public Info(int height, int nodes) {
this.height = height;
this.nodes = nodes;
}
}
/**
```

* 获取两个节点 o1 o2 的最低共同⽗节点2
* 3
*/4
public static Node lca(Node head, Node o1, Node o2) {5
HashMap<Node, Node> fatherMap = new HashMap<>();6
fatherMap.put(head, head);7
process(head, fatherMap);8
HashSet<Node> set1 = new HashSet<>();9

```yaml
Node cur = o1;
while (cur != fatherMap.get(cur)) {
set1.add(cur);
cur = fatherMap.get(cur);
}
set1.add(head);
```

或者：

```java
Node cur2 = o2;
while (cur2!= fatherMap.get(cur2)){
if (set1.contains(cur2)){
return cur2;
}
cur2 = fatherMap.get(cur2);
}
return null;
}
private static void process(Node head, HashMap<Node, Node> fatherMap) {
if (head == null) {
return;
}
fatherMap.put(head.left, head);
fatherMap.put(head.right, head);
process(head.left, fatherMap);
process(head.right, fatherMap);
}
/**
```

* 递归获取两个节点 o1 o2 的最低共同⽗节点2
*/3
public static Node lowestAncestor(Node head, Node o1, Node o2) {4
// base case5
if (head == null || head == o1 || head == o2) {6
return head;7

```java
}
Node left = lowestAncestor(head.left, o1, o2);
Node right = lowestAncestor(head.right, o1, o2);
if (right != null && right != null) {
return head;
}
```

// 左右两棵树  并不是都有返回值14
return left != null ? left : right;15
}16

题⽬四：
在⼆叉树中找到⼀个节点的后继节点。
题⽬：现有⼀种新的⼆叉树节点类型如下：
public class NodeWithParent {1
public int value;2

该结构⽐普通⼆叉树节点结构多了⼀个指向⽗节点的 parent 指针。
假设有⼀个这个节点类型组成的⼆叉树，树中每个节点的 parent 指针都正确的指向⾃⼰的⽗节点，头节点的 parent 指向
null 。
只给⼀个在⼆叉树中的某个节点 node ，请实现返回 node 的后继节点的函数。
在⼆叉树的中序遍历的序列中， node 的下⼀个节点叫做 node 的后续节点。
public NodeWithParent left;3
public NodeWithParent right;4
public NodeWithParent parent;5
public NodeWithParent(int value) {7

```java
this.value = value;
}
}
public static NodeWithParent getSuccessorNode(NodeWithParent node){
if (node == null){
return node;
}
if (node.right!=null){
return getLeftMost(node.right);
}else {
```

题⽬五：
⼆叉树的序列化和反序列化
就是内存⾥的⼀棵树如何变成字符串形式，⼜如何从字符串变成内存⾥的树。
如何判断⼀棵⼆叉树是不是另⼀棵⼆叉树的⼦树？
先序序列化举例：
// ⽆右⼦树8

```
NodeWithParent parent = node.parent;
```

// 当前节点是其⽗亲节点右孩⼦10
while (parent!=null && parent.left!=node){11

```java
node = parent;
parent = node.parent;
}
return parent;
}
}
private static NodeWithParent getLeftMost(NodeWithParent node) {
if (node == null){
return node;
}
while (node.left != null){
node = node.left;
}
return node;
}
```

/**1
* 以 head 为头的树，请序列化成字符串返回2
*/3
public static String serialPre(Node head) {4
if (head == null) {5
return "#_";6

```java
}
String res = head.value + "_";
res += serialPre(head.left);
res += serialPre(head.right);
return res;
}
public static Node reconByPreString(String preStr) {
String[] values = preStr.split("_");
Queue<String> queue = new LinkedList<>();
for (int i = 0; i != values.length; i++) {
queue.add(values[i]);
}
return reconPreOrder(queue);
}
private static Node reconPreOrder(Queue<String> queue) {
```

对折纸条，打印所有折痕的⽅向（⼆叉树的中序遍历）：

```java
String value = queue.poll();
if (value.equals("#")) {
return null;
}
Node head = new Node(Integer.valueOf(value));
head.left = reconPreOrder(queue);
head.right = reconPreOrder(queue);
return head;
}
/**
```

* 以下是折纸问题：2
* 对折纸条，打印所有折痕的⽅向，这其实就是⼀个⼆叉树的中序遍历3
* @param N4
*/5
public static void printAllFolds(int N) {6
printProcess(1, N, true);7
}8
// 递归过程，来到了某⼀个节点9

// i 是节点的层数， n ⼀共的层数， down=true 凹  down=false 凸10
private static void printProcess(int i, int n, boolean down) {11
if (i > n) {12
return;13

```
}
printProcess(i + 1, n, true);
```

System.out.println(down ? " 凹 " : " 凸 ");16
printProcess(i + 1, n, false);17

```java
}
public static void main(String[] args) {
int N = 3;
printAllFolds(N);
}
```
