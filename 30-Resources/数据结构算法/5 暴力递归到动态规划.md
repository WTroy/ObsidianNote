---
title: 5 暴力递归到动态规划
source: 有道云笔记
imported: true
---

题⽬引出：
已知有⼀组数字，每⼀步只能⾛⼀位，可以往左也可以往右，开始节点只能往右，结束节点只能往左。然后给定起始位
置 S 和结束位置 E ，给定总共要⾛多少步 K 。求从 S 到 E 总共有多少种⾛法？
暴⼒递归：
public static int walkWays(int N, int E, int s, int k) {1
return f(N, E, k, s);2
}3
4
/**5
* ⼀共是 1 ～ N 这么多的位置，固定参数6
* 最终的⽬标是 E ，固定参数7
* 还剩 rest 步需要⾛8
* 当前在 cur 位置9
* 返回⽅法数10
*/11
private static int f(int n, int e, int rest, int cur) {12
if (rest == 0) {13
return cur == e ? 1 : 0;14
}15
// rest>0 有路可以⾛16
if (cur == 1) { // 1->217
return f(n, e, rest - 1, 2);18
}19

记忆化搜索：
if (cur == n) {20
return f(n, e, rest - 1, n - 1);21
}22
// 机器⼈来的是中间位置23
return f(n, e, rest - 1, cur - 1) + f(n, e, rest - 1, cur + 1);24
}25
public static int walkWay2(int N, int E, int S, int K) {1
int[][] dp = new int[K + 1][N + 1];2
for (int i = 0; i <= K; i++) {3
for (int j = 0; j <= N; j++) {4
dp[i][j] = -1;5
}6
}7
return f2(N, E, K, S, dp);8
}9
10
private static int f2(int n, int e, int rest, int cur, int[][] dp) {11
if (dp[rest][cur] != -1) {12
return dp[rest][cur];13
}14
// 缓存没命中15

严格表结构的动态规划：
cur 是 0 的时候，直接不考虑因为 cur 的范围是 1~5. cur 是 1 的时候， rest 依赖于它的右上⽅结果， cur 是 5 时， rest 依赖于
它的左上⽅结果，如果 cur 在 1~5 之间，则 rest 依赖于它的左上⽅和右上⽅值的和。
if (rest == 0) {16
dp[rest][cur] = cur == e ? 1 : 0;17
return dp[rest][cur];18
}19
// rest>0 有路可以⾛20
if (cur == 1) {21
dp[rest][cur] = f(n, e, rest - 1, 2);22
} else if (cur == n) {23
dp[rest][cur] = f(n, e, rest - 1, n - 1);24
} else {25
dp[rest][cur] = f(n, e, rest - 1, cur - 1) + f(n, e, rest - 1, cur + 1);26
}27
return dp[rest][cur];28
}29
public static int dpWay(int N, int P, int M, int K) {1
int[][] dp = new int[K + 1][N + 1];// dp[...][0] 废了不⽤2
dp[0][P] = 1;3
for (int rest = 1; rest <= K; rest++) {4
for (int cur = 1; cur <= N; cur++) {5

另⼀个题：
由⼀组正整数，每个数代表着⼀张卡⽚，有⼀个⽬标数字（本题中就是 10 ）求组成这个数字，最少⽤⼏张卡⽚？
if (cur == 1) {6
dp[rest][cur] = dp[rest - 1][2];7
} else if (cur == N) {8
dp[rest][cur] = dp[rest - 1][N - 1];9
} else {10
dp[rest][cur] = dp[rest - 1][cur - 1] + dp[rest - 1][cur + 1];11
}12
}13
}14
return dp[M][K];15
}16
17
public static int ways1(int N, int M, int K, int P) {18
// 参数⽆效直接返回 019
if (N < 2 || K < 1 || M < 1 || M > N || P < 1 || P > N) {20
return 0;21
}22
// 总共 N 个位置，从 M 出发，还剩 K 步，返回最终到达 P 的⽅法数23
return dpWay(N, M, K, P);24
}25
public static int process(int[] arr, int index, int rest) {1
if (rest < 0) {2
return -1;3

记忆化搜索：
}4
if (rest == 0) {5
return 0;6
}7
if (index == arr.length) {8
return -1;9
}10
int p1 = process(arr, index + 1, rest);11
int p2Next = process(arr, index + 1, rest - arr[index]);12
if (p1 == -1 && p2Next == -1) {13
return -1;14
} else {15
if (p1 == -1) {16
return p2Next + 1;17
}18
if (p2Next == -1) {19
return p1;20
}21
return Math.min(p1, p2Next + 1);22
}23
}24

严格表结构：
public static int minCoins2(int[] arr, int aim) {1
int[][] dp = new int[arr.length + 1][aim + 1];2
for (int i = 0; i <= arr.length; i++) {3
for (int j = 0; j <= aim; j++) {4
dp[i][j] = -2;5
}6
}7
return process2(arr, 0, aim, dp);8
}9
// arr[index...] 组成出 aim 这么多钱，最少的硬币数量返回10
private static int process2(int[] arr, int index, int aim, int[][] dp) {11
if (aim < 0) {12
return -1;13
}14
if (dp[index][aim] != -2) {15
return dp[index][aim];16
}17
if (aim == 0) {18
dp[index][aim] = 0;19
} else if (index == arr.length) {20
dp[index][aim] = -1;21
} else {22
int p1 = process2(arr, index + 1, aim, dp);23
int p2Next = process2(arr, index + 1, aim - arr[index], dp);24
if (p1 == -1 && p2Next == -1) {25
dp[index][aim] = -1;26
} else {27
if (p1 == -1) {28
dp[index][aim] = p2Next + 1;29
} else if (p2Next == -1) {30
dp[index][aim] = p1;31
} else {32
dp[index][aim] = Math.min(p1, p2Next + 1);33
}34
}35
}36
return dp[index][aim];37
}38

public static int minCoins3(int[] arr, int aim) {1
int N = arr.length;2
int[][] dp = new int[N + 1][aim + 1];3
for (int row = 0; row <= N; row++) {4
dp[row][0] = 0;5
}6
for (int col = 1; col <= aim; col++) {7
dp[N][col] = -1;8
}9
for (int index = N - 1; index >= 0; index--) {10
for (int rest = 1; rest <= aim; rest++) {11
int p1 = dp[index + 1][rest];12
int p2Next = -1;13
if (rest - arr[index] >= 0) {14
p2Next = dp[index + 1][rest - arr[index]];15
}16
if (p1 == -1 && p2Next == -1) {17
dp[index][rest] = -1;18
} else {19
if (p1 == -1) {20
dp[index][rest] = p2Next + 1;21
}22

总结：
if (p2Next == -1) {23
dp[index][rest] = p1;24
}25
dp[index][rest] = Math.min(p1, p2Next + 1);26
}27
}28
}29
return dp[0][aim];30
}31
递归
记忆是搜索
严格表结构分析
确定可变量
确定最终终⽌位置
确定 basecase
普通位置确定相互依赖
确定严格表依次计算的顺序，以及从什么地⽅开始最终计算到终⽌位置