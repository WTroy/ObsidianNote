---
title: 5 暴力递归到动态规划
source: 有道云笔记
imported: true
---

题⽬引出：
已知有⼀组数字，每⼀步只能⾛⼀位，可以往左也可以往右，开始节点只能往右，结束节点只能往左。然后给定起始位
置 S 和结束位置 E ，给定总共要⾛多少步 K 。求从 S 到 E 总共有多少种⾛法？
暴⼒递归：
public static int walkWays(int N, int E, int s, int k) {1
return f(N, E, k, s);2

```
}
/**
```

* ⼀共是 1 ～ N 这么多的位置，固定参数6
* 最终的⽬标是 E ，固定参数7
* 还剩 rest 步需要⾛8
* 当前在 cur 位置9
* 返回⽅法数10
*/11
private static int f(int n, int e, int rest, int cur) {12
if (rest == 0) {13

```java
return cur == e ? 1 : 0;
}
```

// rest>0 有路可以⾛16
if (cur == 1) { // 1->217
return f(n, e, rest - 1, 2);18
}19

记忆化搜索：
if (cur == n) {20
return f(n, e, rest - 1, n - 1);21
}22
// 机器⼈来的是中间位置23
return f(n, e, rest - 1, cur - 1) + f(n, e, rest - 1, cur + 1);24

```java
}
public static int walkWay2(int N, int E, int S, int K) {
int[][] dp = new int[K + 1][N + 1];
for (int i = 0; i <= K; i++) {
for (int j = 0; j <= N; j++) {
dp[i][j] = -1;
}
}
return f2(N, E, K, S, dp);
}
private static int f2(int n, int e, int rest, int cur, int[][] dp) {
if (dp[rest][cur] != -1) {
return dp[rest][cur];
}
```

// 缓存没命中15

严格表结构的动态规划：
cur 是 0 的时候，直接不考虑因为 cur 的范围是 1~5. cur 是 1 的时候， rest 依赖于它的右上⽅结果， cur 是 5 时， rest 依赖于
它的左上⽅结果，如果 cur 在 1~5 之间，则 rest 依赖于它的左上⽅和右上⽅值的和。
if (rest == 0) {16

```java
dp[rest][cur] = cur == e ? 1 : 0;
return dp[rest][cur];
}
```

// rest>0 有路可以⾛20
if (cur == 1) {21

```java
dp[rest][cur] = f(n, e, rest - 1, 2);
} else if (cur == n) {
dp[rest][cur] = f(n, e, rest - 1, n - 1);
} else {
dp[rest][cur] = f(n, e, rest - 1, cur - 1) + f(n, e, rest - 1, cur + 1);
}
return dp[rest][cur];
}
public static int dpWay(int N, int P, int M, int K) {
```

int[][] dp = new int[K + 1][N + 1];// dp[...][0] 废了不⽤2

```
dp[0][P] = 1;
for (int rest = 1; rest <= K; rest++) {
for (int cur = 1; cur <= N; cur++) {
```

另⼀个题：
由⼀组正整数，每个数代表着⼀张卡⽚，有⼀个⽬标数字（本题中就是 10 ）求组成这个数字，最少⽤⼏张卡⽚？
if (cur == 1) {6

```java
dp[rest][cur] = dp[rest - 1][2];
} else if (cur == N) {
dp[rest][cur] = dp[rest - 1][N - 1];
} else {
dp[rest][cur] = dp[rest - 1][cur - 1] + dp[rest - 1][cur + 1];
}
}
}
return dp[M][K];
}
public static int ways1(int N, int M, int K, int P) {
```

// 参数⽆效直接返回 019
if (N < 2 || K < 1 || M < 1 || M > N || P < 1 || P > N) {20
return 0;21
}22
// 总共 N 个位置，从 M 出发，还剩 K 步，返回最终到达 P 的⽅法数23
return dpWay(N, M, K, P);24

```java
}
public static int process(int[] arr, int index, int rest) {
if (rest < 0) {
return -1;
```

记忆化搜索：

```java
}
if (rest == 0) {
return 0;
}
if (index == arr.length) {
return -1;
}
int p1 = process(arr, index + 1, rest);
int p2Next = process(arr, index + 1, rest - arr[index]);
if (p1 == -1 && p2Next == -1) {
return -1;
} else {
if (p1 == -1) {
return p2Next + 1;
}
if (p2Next == -1) {
return p1;
}
return Math.min(p1, p2Next + 1);
}
}
```

严格表结构：
public static int minCoins2(int[] arr, int aim) {1
int[][] dp = new int[arr.length + 1][aim + 1];2
for (int i = 0; i <= arr.length; i++) {3
for (int j = 0; j <= aim; j++) {4

```java
dp[i][j] = -2;
}
}
return process2(arr, 0, aim, dp);
}
```

// arr[index...] 组成出 aim 这么多钱，最少的硬币数量返回10
private static int process2(int[] arr, int index, int aim, int[][] dp) {11
if (aim < 0) {12
return -1;13

```java
}
if (dp[index][aim] != -2) {
return dp[index][aim];
}
if (aim == 0) {
dp[index][aim] = 0;
} else if (index == arr.length) {
dp[index][aim] = -1;
} else {
int p1 = process2(arr, index + 1, aim, dp);
int p2Next = process2(arr, index + 1, aim - arr[index], dp);
if (p1 == -1 && p2Next == -1) {
dp[index][aim] = -1;
} else {
if (p1 == -1) {
dp[index][aim] = p2Next + 1;
} else if (p2Next == -1) {
dp[index][aim] = p1;
} else {
dp[index][aim] = Math.min(p1, p2Next + 1);
}
}
}
return dp[index][aim];
}
```

public static int minCoins3(int[] arr, int aim) {1

```java
int N = arr.length;
int[][] dp = new int[N + 1][aim + 1];
for (int row = 0; row <= N; row++) {
dp[row][0] = 0;
}
for (int col = 1; col <= aim; col++) {
dp[N][col] = -1;
}
for (int index = N - 1; index >= 0; index--) {
for (int rest = 1; rest <= aim; rest++) {
int p1 = dp[index + 1][rest];
int p2Next = -1;
if (rest - arr[index] >= 0) {
p2Next = dp[index + 1][rest - arr[index]];
}
if (p1 == -1 && p2Next == -1) {
dp[index][rest] = -1;
} else {
if (p1 == -1) {
dp[index][rest] = p2Next + 1;
}
```

总结：
if (p2Next == -1) {23

```java
dp[index][rest] = p1;
}
dp[index][rest] = Math.min(p1, p2Next + 1);
}
}
}
return dp[0][aim];
}
```

递归
记忆是搜索
严格表结构分析
确定可变量
确定最终终⽌位置
确定 basecase
普通位置确定相互依赖
确定严格表依次计算的顺序，以及从什么地⽅开始最终计算到终⽌位置
