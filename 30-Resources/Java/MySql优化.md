---
title: MySql优化
source: 有道云笔记
imported: true
related: ["MYSQL技术内幕 InnoDB存储引擎", "Mysql主从+集群", "事务：隔离级别，不生效场景", "Mysql事务，事务的隔离级别", "视图 V_FULE_INVOICE_RECEIPT_COSTS 的优化"]
---

常⻅的索引模型：哈希表
以上给出的索引算法只适合等于查询，不适合范围查询。内存型数据库⽤的⽐较多。
常⻅的索引模型：有序数组
常⻅索引模型：⼆叉搜索树
1. Sql 优化
1.1 索引
插⼊极快
范围检索不⾏
⽀持范围检索
修改数据不⾏
查询增删⽐较均衡
但是主流数据库没⼈⽤他，因为它是⼆叉树，数据过多会导致树⾼很⾼。

常⻅索引模型：N 叉搜索树
Innodb 就是⽤了这种索引模型，⻅下图：
主键索引包含数据，数据⾏就在主键索引上，叶⼦节点包含数据列（聚簇索引： innodb 中的主键索引）
辅助索引会通过主键索引去检索，叶⼦节点包含对应的主键值
如果语句是  select * from emp where ID = 500, 即主键查询⽅式，则只需要索引 ID 这颗 B+ 树
如果语句是  select * from emp where empno=5, 即普通索引查询⽅式，则需要先索引 k 索引树，得到 ID 的值 500 ，
再到 ID 索引树搜索⼀次，这个过程称之为回表。

B+ 树为了保证索引的有序性，在增加数据的时候要进⾏索引维护：
但是如果插⼊数据为 400 ，怎会引起⼤量的树节点位置变化；如果数据⻚满了还会引起⻚分裂，如下图：
1.1.1 索引的维护
上边插⼊值为 700 的数据，不会影响树结构
解决⽅案：增加⾃增主键
可以避免出现索引⻚分裂：不涉及挪动其他记录，也不会导致叶⼦节点分裂
如果⽤整型做主键，则只要 4 个字节；如果⽤⻓整型（ bigint ）则需要 8 个字节。

⾃增主键的插⼊：
如果只有⼀个索引，并且该索引必须是唯⼀索引：那就⽤这个索引作为主键索引。
具体流程：
1.1.2 特例：
1.2 索引的优化策略
1.2.1 范围检索
oracle 会使⽤双向链表去检索，如上图绿⾊所示

```bash
mysql 也有这个链表，但是它不⽤；因为 mysql 是多存储引擎，所以不使⽤双向链表
mysql 会每次从 root 节点去⼀个⼀个查询，所以范围查询效率低于 oracle
```

都会设计回表操作； BTW ：如果全表扫描，在 Mysql ⾥也是⼀条⼀条的扫描。
范围查询的优化⼿段：覆盖索引
创建联合索引  (name,age)
索引项是根据索引定义⾥边出现的字段顺序排序的：
减少回表，不⾛主键索引：使⽤复合索引；不要使⽤ select * （查什么数据⽤什么符合索引； select 什么）
type ： range ref 算索引覆盖； index 是索引扫描； all 是全表扫描
type ： index 和  all 都不是最好的，有优化的余地
Extra ： Using index
1.2.2 最左前缀原则
联合索引将 name 和 age 拼起来进⾏存储
决定索引中先后的是 name ，如果 name 相同，则 age ⽣效

以下 4 ， 5 的都不⾛以上复合索引，因为 mysql 最左前缀原则（ oracle 有索引跳跃，其实可以）， 6 因为有查询转换，也
可以⾛索引：
索引下推：索引中第⼀项属于模糊查询，并 % 在后边，则第⼆个索引也可以⽣效，⽐如下边的这个查询：
索引下推的优化开关的打开⽅式：
除了主键，建议建⽴⾮唯⼀索引：
Mysql5.6 之前，只有第⼀列⽣效，即 name
5.6 之后会⽣效两个：如果左边的列是范围查询，则将左前缀下推到下⼀级索引，减少回表
1.2.3 实战中需要注意的点

更新性能分析：
innodb 引擎的 change buffer ：
尽量使⽤普通索引：

```sql
union 和  union all 各⾃的语句仍然⾛索引，区别在于合并结果时是否去重。
```

如果没有索引， mysql 会给到默认的优化⽅案：临时表：
唯⼀索引修改时需要判断唯⼀性：内存相差微乎其微
普通索引修改时不需要读取记录
更新的信息会放在⼀个 change buffer 中，当需要查询的时候，出发 merge 操作或者数据库关闭的时候才会修改；⼆
次修改的时候，会直接修改 change buffer 中的数据，也不会更新磁盘上的数据。减少 io 次数。
如果数据直接就在内存中，则直接更新
如果修改的列有唯⼀属性，则直接更新不会进⼊ change buffer ，并且更新对应的唯⼀索引
change buffer 在账单类业务，⽇志类业务；写多读少的业务。如果⼀插⼊就要读取的情况下不要开启 change buffer
1.2.4 索引的选择
Mysql 中的 not in 和不等于都不会⾛索引
转换都是字符转数字：如果 id 是数字，但是查询条件是字符，则仍然⾛索引；反之则不然
1.2.5 union 和  union all

如果 group by 的字段上有索引：
如果没有索引，则通过临时表：
优化 group by 的⽅法，加上索引。拿上边举例⼦就是加上 ( 国家，⼈⼝ ) 这样的索引：
加索引之前：
1.2.6 group by

加上索引之后：索引中有国家名并且有⼈⼝数量

```sql
select count(*) from t 随着数据量越来越⼤，会越来越慢！
```

count(*) 的实现⽅法：
innoDB 代替 MyISAM 引擎，是因为它⽀持事务。   他要保证⼀个事务开启的时候读取到的数据量在事务结束之前保持⼀
致，所以 innodb 才有了这种⽅式。
show table status 速度快，但是不准确，抽样采集：误差 40% 。
缓存系统保存计数：
1.3 Mysql 的计数
1.3.1 计数的实现⽅式

⽤⼀致性表保存计数：
count 的⽤法⽐较：
⽤户表：
redis 宕机：问题不⼤
数据不⼀致问题：更新数据库的 redis 过程中这段时间会产⽣数据不⼀致
count( 主键 id)
count(1) 只遍历不取值
count( 字段 )
count(*): 和 count(1) ⼀致
结论：
1.4 给字符串加索引

对邮箱建⽴索引的话，因为⻓字段会导致索引占⽤空间巨⼤。
解决⽅案：前缀索引：
因为截取字段偏短，会导致  查出来很多条数据，导致⼤量回表。
改进⽅案：选取合适的⻓度
前缀索引会导致覆盖索引失效，仍然需要回表查找。
身份证号：⽆区分度
订单号：⽆区分度
产品标签：⽆区分度
不适⽤的场景我就要⽤：
1.4.1 前缀索引对覆盖索引的影响
1.4.2 前缀索引不适合的场景

有的公司不让使⽤ join ，为什么？
⼤⼩不同的两个表连接，哪个表做驱动表⽐较好？
倒序的话：范围检索就没⽤了
reverse 不能加在列名上，不然索引失效
1.5 join 的使⽤
算法单⼀：嵌套循环
如果能达到被驱动表中有索引  则可以⽤，不然就在 app 中做处理。不然就是两个全表扫描，时间复杂度：两表⾏数
相乘（ Simple Nested-Loop Join ）
如果连接条件中⼤表有索引，则 mysql 会优化让有索引的⼤表来作为驱动表
执⾏计划中，谁在上边谁就是驱动表
驱动表会全表扫描，所以⼩表作为驱动表最好

```bash
mysql 会⾃⼰根据统计信息来确定驱动表（默认）
```

强制让前边的表作为驱动表

什么是⼩表？
如果两表⾏数⼀致，则查询列少的则作为⼩表：
通过存储过程，向数据库表中添加 1000 ⾏数据：

```bash
mysql 中 join 最好的情况：驱动表全表扫描，被驱动的表索引扫描
mysql 在估算数据量的时候，有索引的列才会正确
```

如果 where 没有索引，如果 **=** 的话会给 10% ，如果是 **> 等 ** 会给 33.33%

```sql
create table t1 like t2; // 只拷⻉表结构
insert into t1(select * from t2 where ...); // 插⼊数据
```

两种表连接的⽅法：
Using where: 将数据集放到内存中做过滤且不⾛索引。
Using join buffer BNL 算法， mysql 针对没有索引的表连接的优化算法：
t1 join t2 on t1.id=t2.id
t1,t2 where t1.id=t2.id
每个⽤户都有⼀个内存空间叫： join buffer
驱动表放⼊  buffer ⾥，被驱动表再去扫描
优化点：驱动表在内存中，处理快
如果驱动表过⼤，则驱动表分多次进⼊ buffer 进⾏处理，此算法名字的由来
oracle 中的hash join ， mysql8.0 之后会⽀持，之前版本都是被驱动表全表扫描

优化原则：
1.5.1 多表连接 join 的⽅法
考虑表的⼤⼩
考虑表之间的连接关系
优化原则：最⼩的表和谁有连接条件谁就是下⼀层的驱动表
1.5.2 外连接
左外连接：驱动表就是 a

上图导致外连接变内连接：on 执⾏完毕之后， where 再执⾏ -> 外连接变内连接  join
Extra ： Using filesort ：
如果数据过⼤， sort buffer 按块排序；各个块内有序，但是块之间⽆序。然后进⾏堆排序，谁⼩谁进⼊ sort buffer 。它
必然涉及硬盘排序或者内存排序。要优化就要去掉 using filesort ，⽽出现 using index。
右外连接：驱动表就是 b
1.6 排序优化

解决⽅案：
Mysql 如何得到基数：
N ， M 分别是多少：
⼿动收集，不会阻塞任何操作，可以常⽤：

```sql
order by 后边的列是索引列
select 的列必须是索引列
```

排序的索引也要复合最左原则
索引下推对 order by ⽆效
1.6.1 Mysql 统计信息相关

全级锁：只有⼀个语句会加上全局锁
数据库处于只读状态，所有更新语句被阻塞，数据定义被阻塞， commit rollback 被阻塞。
表级锁：
1. 表锁：

```yaml
t1: ⼤家都只能读； t2: 除了我能读写，其他什么都不能。
```

释放： unlock tables
2. 元数据锁（ metadata lock MDL ）
⽬的：保证读写的正确性
特点：不需要显式使⽤，在访问⼀个表的时候会⾃动加上
MDL 读锁：读之间相互兼容； MDL 写锁：独占 .
* 某公司因为更改⼩表的字段导致数据库宕机：
1.7 Mysql 中的锁

以上场景因为会话 1 有事务，会话 3 加写锁导致加不上，从此 mysql 这张表变成了串⾏操作的数据库。
这个问题的解决⽅案：
⾏级锁：
等待补充！
并发下数据库应该关注的点：
数据库中并发的设置：  max_connections
并发的限制（操作系统级别）：
获取不到。直接结束（版本 8.0 以后）
获取不到等 N 秒（版本 8.0 以后）
2. 系统优化及主从复制半同步
2.1 并发
可⽤内存
线程总和
每个连接消耗的内存量
每个连接的负载
响应时间
2.1.1 并发对 CPU 的影响

并发的监控： show global status like 'connections';
监控有多少连接数：
Mysql 的内存结构：
Threads_connected ：真实连接数
Threads_running ：正在运⾏的连接 ( ⼤查询，被阻塞的语句 ) ，⼤查询会使⽤ cpu 。这个才是⾼并发真正的考验点；
被阻塞的连接数查询⽅法：
设置 mysql 的并发线程量： 0 表示不做设置，经验之谈 32 核 CPU 设置为 128
2.1.2 并发对内存的影响

内存使⽤量的计算⽅式：
全局内存在服务启动的时候就会分配，不会再发⽣变换，内存暴增只能是 thread 中使⽤量激增。 mysql 为了更好的管理
内存，在机器内存和⽤户内存之间有⼀个内存缓冲池，缓冲池采⽤的是贪婪算法，使⽤完不会将内存还给操作系统。
这是 mysql 管理内存的三种⽅式： ptmalloc 是 mysql 默认的实现⽅式。
内存激增的场景：
第⼀波⾼并发打满内存缓冲池。第⼆波⾼并发从缓冲池取内存， libcmalloc 响应速度慢的话， mysql 会并⾏的跟操作系统
要内存。导致内存缓冲池⽆限增⼤，并且贪婪算法不释放。要释放：重启服务。
根本问题是：短链接⻛暴。
2.1.3 mysql 的体系架构

innodb buffer poor ：
free list 总会⽤完，则进⼊ LRU ：最新数据都在链表头：
2.1.4 mysql 的两个监控

InnoDB 对 LRU 的优化：
防⽌热点数据被刷出：
脏⻚（更新，但是没写⼊磁盘）的管理：链表将脏⻚串起来， flush list 中的数据不能被替换：

如果更新太快，写⼊磁盘过慢， lru list 和 flush list 相同的时候，数据库就卡死了。
innodb log buffer ：
使⽤⽇志的位置来表示 redo 的执⾏顺序：
如果 log buffer 满了，则会从头重新开始覆盖：覆盖的前提是被覆盖的位置的数据已经被存⼊磁盘。并且 LSN 的值会递
增不会重置。
checkpoint：⽤来记录 redo ⽇志的操作位置。他被存储在 logfile 头部中。 checkpoint 随着写⼊脏数据更新值。
如果脏数据写⼊磁盘的速度偏低，未写⼊的 logfile 写满了 log buffer ，数据库卡死。

上述两种数据卡卡死的原因都是：脏数据写⼊磁盘太慢：
如果卡死： mysql 会只允许 write_thread 写⼊脏数据到磁盘，其他都暂停。
write_thread 写⼊磁盘会根据服务器的 IO 情况来写⼊。
上线服务器必改参数：告诉 write_thread 我服务器的 IO 能⼒
⽆损复制 - 主库：
默认 200 ，就是笔记本电脑的⽔平
推荐服务器 IO 的 IOPS 值：机械硬盘  2000-5000 ； SSD 20000-30000
2.2 主从复制，半同步复制

上图中
第⼀种：如果提交成功还没同步到从库，主库挂了，则会导致当前提交的数据丢失；
第⼆种：半同步复制。提交成功说明从库收到⽇志，但是不保证当前已经存⼊库中。⼀主多从，只需要保证⼀个待切换
的从能够半同步即可。
第三个：组复制。乐观锁定，所有节点最后都会做冲突检测，然后再统⼀提交。
半同步是以插件的形式存在；  show plugins
安装步骤：
2.2.1 配置半同步

查看状态：
如果半同步失败，则会降级为普通同步
2.2.2 半同步复制产⽣的幻读现象

after_commit 模式下的幻读和数据丢失：
因为 mysql 的实现逻辑如下：
不要设置为： after_commit

同⼀个表更新必须顺序；不同的表则并⾏执⾏。
2.3 并⾏复制（原理）
2.3.1 主从同步的过程
2.3.2 解决⽅案（阿⾥ sql ）

在⼀个逻辑时间：纳秒内能够同时提交的事务，肯定不会有冲突，按照逻辑时间分组，然后写⼊ binlog ，从库就可以直
接使⽤多个线程来处理⼀个分组中的不同事务。⼀组事务结束才能开始下⼀组事务。
2.3.3 mysql5.6 多线程同步
2.3.4 mysql5.7 的优化

从库的并⾏的复制：
2.4 并⾏复制（设置）
3. 应急处理
3.1 短链接⻛暴

当连接池中的⻓链接被占⽤完（ bug ），则会有⼤量短链接产⽣。
解决⽅案：
1. 设置 mysql 最⼤连接数 ( 下策 ) ：会有额外给⼀个名额， root ⽤户在本地登陆
2. 处理掉占着连接不⼯作的连接
3. 减轻连接造成的压⼒（ 8.0 版本之后⽆效），直接加在 my.cnf 重启
show processlist /G; 查看 mysql 的连接： sleep 状态并且没有事务 , 如果有失误杀掉失误会回滚
status ：查看本连接的状态
正在进⾏的事务： select * from information_schema.innodb_trx\G;
设置参数 wait_timeout: show variables like 'wait_timeout';
导致密码失效等

⾏锁：是谁搞的，⼲掉。
慢 sql 优化：
解决⽅案：
1. 如何紧急创建索引（最古⽼，最有效）
⽅法⼀：
有任何语句在运⾏时，不能对索引进⾏操作： DML 元数据锁（读）：
从库创建索引 - 主从切换 - 原主库添加索引
⽅法⼆：
query rewrite, 针对源语句⽆法修改的 sql 优化。版本 5.7 以后。开启：
会新增⼀个库： query_rewrite
3.2 ⻓连接

```bash
# mysql -uroot -pmysql < install_rewriter.sql
mysql> show variables like 'rewriter_enabled';
mysql> use db1;
```

如何避免：
慢查询⽇志查询：
2. QPS 激增

```sql
mysql> insert into
query_rewrite.rewrite_rules(pattern,replacement,pattern_database) values("select
* from t where id-1=0","select 9","db1");
mysql> CALL query_rewrite.flush_rewrite_rules();
mysql> show warnings;
# mysql -uroot -pmysql < uninstall_rewriter.sql
```

⽩名单机制：如果⽩名单库中有你才能去操作业务库
删除或者锁定⽤户
如果上边这些都没有，就使⽤ query rewrite （不推荐）

---
## 相关笔记
- [[MYSQL技术内幕 InnoDB存储引擎]]
- [[Mysql主从+集群]]
- [[事务：隔离级别，不生效场景]]
- [[Mysql事务，事务的隔离级别]]
- [[视图 V_FULE_INVOICE_RECEIPT_COSTS 的优化]]
