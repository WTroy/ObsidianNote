---
title: 常用命令
source: 有道云笔记
imported: true
---

将远程服务器的路径挂载到本地路径上： mount ip:/home/work/kmpro_home/shared / 本地路径
查看具体⽂件夹的占⽤情况：du --max-depth=1 -h
增加路由：  route add  10.1.21.231 gw 10.10.10.18
查看端⼝进程： netstat -anp|grep 7140
查看端⼝状态： nc -z localhost 7000-9000
查找包含某字段的所有⽂件： grep -r CZTChannel *
查看相关进程： ps -aux | grep redis
查看

```bash
tar -tf aaa.tar.gz    在不解压的情况下查看压缩包的内容
```

压缩

```bash
tar –cvf jpg.tar *.jpg // 将⽬录⾥所有 jpg ⽂件打包成 tar.jpg
tar –czf jpg.tar.gz *.jpg // 将⽬录⾥所有 jpg ⽂件打包成 jpg.tar 后，并且将其⽤ gzip 压缩，⽣成⼀个 gzip 压缩过的包，命
```

名为 jpg.tar.gz

```bash
tar –cjf jpg.tar.bz2 *.jpg // 将⽬录⾥所有 jpg ⽂件打包成 jpg.tar 后，并且将其⽤ bzip2 压缩，⽣成⼀个 bzip2 压缩过的包，
```

命名为 jpg.tar.bz2

```bash
tar –cZf jpg.tar.Z *.jpg   // 将⽬录⾥所有 jpg ⽂件打包成 jpg.tar 后，并且将其⽤ compress 压缩，⽣成⼀个 umcompress 压
```

缩过的包，命名为 jpg.tar.Z
解压

```bash
tar –xvf file.tar // 解压  tar 包
tar -zxvf file.tar.gz // 解压 tar.gz
tar -jxvf file.tar.bz2   // 解压  tar.bz
tar –Zxvf file.tar.Z // 解压 tar.Z
```

JDK 配置：  /etc/profile

```yaml
JAVA_HOME=/usr/lib/jdk1.8.0_101
CLASSPATH=.:$JAVA_HOME/lib/tools.jar:$JAVA_HOME/lib/dt.jar
PATH=$JAVA_HOME/bin:$HOME/bin:$HOME/.local/bin:$PATH
export PATH JAVA_HOME CLASSPATH
```
