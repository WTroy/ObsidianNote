---
title: 接口幂等性
source: 有道云笔记
imported: true
related: ["SpringCloud", "Kubernetes", "Redis项目实战与分布式集群", "事务：隔离级别，不生效场景", "Kafka"]
---

1. 接⼝调⽤存在的问题
现如今我们的系统⼤多拆分为分布式SOA，或者微服务，⼀套系统中包含了多个⼦系统服务，⽽⼀个⼦系统服务往往会去
调⽤另⼀个服务，⽽服务调⽤服务⽆⾮就是使⽤RPC通信或者restful，既然是通信，那么就有可能在服务器处理完毕后返回结果
的时候挂掉，这个时候⽤户端发现很久没有反应，那么就会多次点击按钮，这样请求有多次，那么处理数据的结果是否要统⼀
呢？那是肯定的！尤其在⽀付场景。

2. 什么是接⼝幂等性
接⼝幂等性就是⽤户对于同⼀操作发起的⼀次请求或者多次请求的结果是⼀致的，不会因为多次点击⽽产⽣了副作⽤。举
个最简单的例⼦，那就是⽀付，⽤户购买商品后⽀付，⽀付扣款成功，但是返回结果的时候⽹络异常，此时钱已经扣了，⽤户再
次点击按钮，此时会进⾏第⼆次扣款，返回结果成功，⽤户查询余额返发现多扣钱了，流⽔记录也变成了两条．．．,这就没有保
证接⼝的幂等性

3. 什么情况下需要保证接⼝的幂等性
在增删改查4个操作中，尤为注意就是增加或者修改，

```yaml
A: 查询操作
```

查询对于结果是不会有改变的，查询⼀次和查询多次，在数据不变的情况下，查询结果是⼀样的。select是天然的幂等操作

```yaml
B: 删除操作
```

删除⼀次和多次删除都是把数据删除。(注意可能返回结果不⼀样，删除的数据不存在，返回0，删除的数据多条，返回结果
多个,在不考虑返回结果的情况下,删除操作也是具有幂等性的)

```yaml
C: 更新操作
```

修改在⼤多场景下结果⼀样,但是如果是增量修改是需要保证幂等性的,如下例⼦:
把表中id为XXX的记录的A字段值设置为1,这种操作不管执⾏多少次都是幂等的
把表中id为XXX的记录的A字段值增加1,这种操作就不是幂等的

```yaml
D: 新增操作
```

增加在重复提交的场景下会出现幂等性问题,如以上的⽀付问题

4. 那么如何设计接⼝才能做到幂等呢？
常⻅的两种实现⽅案:  1. 通过代码逻辑判断实现    2. 使⽤token机制实现      下⾯以⽀付系统为例,分别对接⼝的幂等性进
⾏说明与实现

```yaml
A: 通过代码逻辑判断实现接⼝幂等性,只能针对⼀些满⾜判断的逻辑实现,具有⼀定局限性
```

⽤户购买商品的订单系统与⽀付系统；订单系统负责记录⽤户的购买记录已经订单的流转状态（orderStatus),⽀付系统⽤于
付款，提供如下接⼝，订单系统与⽀付系统通过分布式⽹络交互。
boolean pay(int accountid,BigDecimal amount) // ⽤于付款，扣除⽤户的
这种情况下，⽀付系统已经扣款，但是订单系统因为⽹络原因，没有获取到确切的结果，因此订单系统需要重试。由上图可⻅，
⽀付系统并没有做到接⼝的幂等性，订单系统第⼀次调⽤和第⼆次调⽤，⽤户分别被扣了两次钱，不符合幂等性原则（同⼀个订
单，⽆论是调⽤了多少次，⽤户都只会扣款⼀次）。如果需要⽀持幂等性，付款接⼝需要修改为以下接⼝：
boolean pay(int orderId,int accountId,BigDecimal amount)
通过orderId来标定订单的唯⼀性，付款系统只要检测到订单已经⽀付过，则第⼆次调⽤不会扣款⽽会直接返回结果：
在不同的业务中不同接⼝需要有不同的幂等性，特别是在分布式系统中，因为⽹络原因⽽未能得到确定的结果，往往需要⽀持接
⼝幂等性。
随着分布式系统及微服务的普及，因为⽹络原因⽽导致调⽤系统未能获取到确切的结果从⽽导致重试，这就需要被调⽤系统具有
幂等性。例如上⽂所阐述的⽀付系统，针对同⼀个订单保证⽀付的幂等性，⼀旦订单的⽀付状态确定之后，以后的操作都会返回
相同的结果，对⽤户的扣款也只会有⼀次。这种接⼝的幂等性，简化到数据层⾯的操作：

```sql
update userAmount set amount = amount - 'value' ,paystatus = 'paid' where orderId= 'orderid' and paystatus = 'unpay'
```

其中 value 是⽤户要减少的订单， paystatus 代表⽀付状态， paid 代表已经⽀付， unpay 代表未⽀付， orderid 是订单号。

在上⽂中提到的订单系统，订单具有⾃⼰的状态（ orderStatus), 订单状态存在⼀定的流转。订单⾸先有提交（ 0 ），付款中
（ 1 ），付款成功（ 2 ），付款失败（ 3 ），简化之后其流转路径如图：
当 orderStatus = 1 时，其前置状态只能是 0 ，也就是说将 orderStatus 由 0->1 是需要幂等性的

```sql
update Order set orderStatus = 1 where OrderId = 'orderid' and orderStatus =
```

当orderStatus 处于0，1两种状态时，对订单执⾏0->1 的状态流转操作应该是具有幂等性的。这时候需要在执⾏update操

```
作之前检测orderStatus是否已经=1，如果已经=1则直接返回true即可。
但是如果此时orderStatus = 2,再进⾏订单状态0->1 时操作就⽆法成功，但是幂等性是针对同⼀个请求的，也就是针对同
```

⼀个requestid保持幂等。
这时候再执⾏

```sql
update Order set orderStatus = 1 where OrderId = 'orderid' and orderStatus =
```

接⼝会返回失败，系统没有产⽣修改，如果再发⼀次，requestid是相同的，对系统同样没有产⽣修改。

```yaml
B: 使⽤token机制实现接⼝幂等性,通⽤性强的实现⽅法
```

token机制实现步骤:
1. ⽣成全局唯⼀的token,token放到redis或jvm内存,token会在⻚⾯跳转时获取.存放到pageScope中,⽀付请求提交先获
取token
2. 提交后后台校验token，执⾏提交逻辑,提交成功同时删除token，⽣成新的token更新redis ,这样当第⼀次提交后token
更新了,⻚⾯再次提交携带的token是已删除的token后台验证会失败不让提交
token特点：   要申请，⼀次有效性，可以限流
注意： redis要⽤删除操作来判断token，删除成功代表token校验通过，如果⽤select+delete来校验token，存在并发问
题，不建议使⽤

---
## 相关笔记
- [[SpringCloud]]
- [[Kubernetes]]
- [[Redis项目实战与分布式集群]]
- [[事务：隔离级别，不生效场景]]
- [[Kafka]]
