---
title: init sql
source: 有道云笔记
imported: true
related: ["菜单维护", "Docker 达梦数据库", "统一身份认证", "备份开账存储过程", "近期的SQL脚本"]
tags: ["数据库初始化", "达梦数据库", "用户权限系统", "SQL脚本"]
summary: CEMS系统用户、角色、权限及菜单表的初始化SQL脚本。
---

⻆⾊

```sql
drop table "CEMS"."SYS_USER";
CREATE TABLE "CEMS"."SYS_USER"
(
"USER_ID" BIGINT AUTO_INCREMENT NOT NULL,
"USERNAME" VARCHAR(50) NOT NULL,
"PASSWORD" VARCHAR(100),
"REAL_NAME" VARCHAR(50),
"EMAIL" VARCHAR(100),
"PHONE" VARCHAR(20),
"STATUS" TINYINT DEFAULT 1,
"IS_DELETED" TINYINT DEFAULT 0,
"CREATE_TIME" DATETIME(6) DEFAULT CURRENT_TIMESTAMP,
"UPDATE_TIME" DATETIME(6),
"ROLE_IDS" VARCHAR(200),
PRIMARY KEY("USER_ID"));
CREATE OR REPLACE UNIQUE  INDEX "CEMS"."USER_USERNAME_INDEX" ON "CEMS"."SYS_USER"
("USERNAME" ASC);
COMMENT ON TABLE "CEMS"."SYS_USER" IS ' ⽤户表 ';
COMMENT ON COLUMN "CEMS"."SYS_USER"."ROLE_IDS" IS ' ⻆⾊ ID 集合 “;” 分割 ';
INSERT INTO "CEMS"."SYS_USER"
("USERNAME","PASSWORD","REAL_NAME","EMAIL","PHONE","STATUS","IS_DELETED","UPDATE_
TIME","ROLE_IDS" )
VALUES ('IT_WANGHAO','NoUse',' 王浩 ','IT_WANGHAO@CEAIR.COM','15568638562','1','0',null,'');
drop table "CEMS"."SYS_ROLE";
CREATE TABLE "CEMS"."SYS_ROLE"
(
"ROLE_ID" BIGINT AUTO_INCREMENT NOT NULL,
"ROLE_CODE" VARCHAR(50),
"ROLE_NAME" VARCHAR(50) NOT NULL,
"ROLE_DESC" VARCHAR(255),
"STATUS" TINYINT DEFAULT 1,
"IS_DELETED" TINYINT DEFAULT 0,
"CREATE_TIME" DATETIME(6) DEFAULT CURRENT_TIMESTAMP,
```

权限
菜单
"PERM_IDS" VARCHAR(200),11
PRIMARY KEY("ROLE_ID"));12

```sql
CREATE OR REPLACE UNIQUE  INDEX "CEMS"."ROLE_CODE_INDEX" ON "CEMS"."SYS_ROLE"
("ROLE_CODE" ASC);
COMMENT ON TABLE "CEMS"."SYS_ROLE" IS ' 系统⻆⾊表 ';
COMMENT ON COLUMN "CEMS"."SYS_ROLE"."PERM_IDS" IS ' ⻆⾊对应的权限 ID 集合， “;” 分割 ';
INSERT INTO "CEMS"."SYS_ROLE"
("ROLE_CODE","ROLE_NAME","ROLE_DESC","STATUS","IS_DELETED","PERM_IDS" )
VALUES ('ADMIN',' 管理员 ',' 初始化⻆⾊，系统管理员 ','1','0',null);
CREATE TABLE "CEMS"."SYS_PERMISSION"
(
"PERM_ID" BIGINT AUTO_INCREMENT NOT NULL,
"PERM_CODE" VARCHAR(200) NOT NULL,
"PERM_NAME" VARCHAR(50) NOT NULL,
"PERM_TYPE" TINYINT,
"STATUS" TINYINT DEFAULT 1,
"IS_DELETED" TINYINT DEFAULT 0,
"CREATE_TIME" DATETIME(6) DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY("PERM_ID"));
CREATE OR REPLACE UNIQUE  INDEX "CEMS"."PERMISSION_CODE_INDEX" ON
"CEMS"."SYS_PERMISSION"("PERM_CODE" ASC);
CREATE TABLE "CEMS"."SYS_MENU"
(
"MENU_ID" BIGINT AUTO_INCREMENT NOT NULL,
"PARENT_ID" BIGINT DEFAULT 0,
"MENU_NAME" VARCHAR(50) NOT NULL,
"MENU_TYPE" TINYINT,
"ICON" VARCHAR(50),
"PATH" VARCHAR(100),
"COMPONENT" VARCHAR(100),
"ORDER_NUM" INT DEFAULT 0,
"STATUS" TINYINT DEFAULT 1,
"IS_DELETED" TINYINT DEFAULT 0,
"CREATE_TIME" DATETIME(6) DEFAULT CURRENT_TIMESTAMP,
```

PRIMARY KEY("MENU_ID"));14

```sql
COMMENT ON TABLE "CEMS"."SYS_MENU" IS ' 菜单权限表 ';
COMMENT ON COLUMN "CEMS"."SYS_MENU"."MENU_TYPE" IS '1 ：菜单； 2 ：按钮 ';
sys_menu.csv1.23KB
```

---
## 相关笔记
- [[菜单维护]]
- [[Docker 达梦数据库]]
- [[统一身份认证]]
- [[备份开账存储过程]]
- [[近期的SQL脚本]]
