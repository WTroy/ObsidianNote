---
title: 油价信息维护：每月27号检查维护
source: 有道云笔记
imported: true
---

select trunc(flight_bj_date), count(1)1
from kb_v_flight_fuelinfo i2
where to_char(i.flight_bj_date, 'yyyymm') = '202507'3
group by trunc(flight_bj_date)4
order by trunc(flight_bj_date);5
6
-- 查询主航班数据7
select p.flight_date, count(1)8
from dc_flight_plan p9
where to_char(p.flight_date, 'yyyymm') = '202507'10
group by p.flight_date11
order by p.flight_date;12
13
-- 查询在航班表不存在的机场（有数据需要维护）14
SELECT R.Dept_Airport15
FROM DC_FLIGHT_PLAN DC16
INNER JOIN T_FUEL_INVOICE_RECEIPT R17
ON (DC.FLIGHT_DATE = R.REFUELING_DATE AND DC.FLIGHT_NO = R.FLIGHT_NO AND18
DC.TAIL_NO = R.TAIL_NO AND DC.ORI_ENG = R.DEPT_AIRPORT AND19
DC.DES_ENG = R.ARRI_AIRPORT)20
INNER JOIN T_FUEL_INVOICE F21
ON F.PROXYID = R.INVOICE_NO22
WHERE to_char(FLIGHT_DATE, 'yyyymm') = '202507'23
AND DC.FLT_STATUS <> ' 取消 '24
AND DC.SRV_TYPE NOT IN ('A', 'F', 'H')25
AND NOT EXISTS26
(SELECT 1 FROM T_AIRPORT A WHERE R.Dept_Airport = A.AIRPORT_CODE3);27
28
SELECT DISTINCT P.ORI_ENG29
FROM DC_FLIGHT_PLAN P30
WHERE to_char(FLIGHT_DATE, 'yyyymm') = '202507'31
AND NOT EXISTS32
(SELECT 1 FROM T_AIRPORT A WHERE P.ORI_ENG = A.AIRPORT_CODE3)33
ORDER BY P.ORI_ENG;34
35
36
37
-- 查询机号信息是否存在（有数据需要维护）38
SELECT DISTINCT C.TAIL_NO39

FROM DC_FLIGHT_PLAN C40
WHERE to_char(FLIGHT_DATE, 'yyyymm') = '202507'41
AND NOT EXISTS42
(SELECT 143
FROM T_AIRCRAFT A44
WHERE C.TAIL_NO = A.TAIL_NO45
AND C.FLIGHT_DATE BETWEEN A.APPLY_DATE AND A.EXPIRE_DATE)46
AND C.TAIL_NO IS NOT NULL47
AND C.Flt_Status != ' 取消 '48
ORDER BY C.TAIL_NO;49
50
51
-- 查询国内不存在油价的机场（有数据需要维护）每⽉ 27 号检查维护52
SELECT DISTINCT a.cname || ' 的 ' ||53
decode(P.FLIGHT_TYPE, 'D', ' 国内段 ', 'I', ' 国际段 ') CONTENT54
FROM dc_flight_plan p, T_AIRPORT A55
WHERE p.ori_eng = A.AIRPORT_CODE356
AND to_char(FLIGHT_DATE, 'yyyymm') = '202507'57
AND A.AREA_CODE = 'DOME'58
and p.flt_status not in (' 取消 ')59
AND NOT EXISTS60
(SELECT *61
FROM T_FUEL_DOM_UNIT_PRICE P, T_FUEL_DOM_PRICE_AIRPORT A62
WHERE P.PROXYID = A.DOM_UNIT_PRICE_ID63
AND A.AIRPORT = P.ORI_ENG64
AND P.FLIGHT_TYPE = DECODE(P.FLIGHT_TYPE,65
'D',66
'DOME',67
'I',68
'INTL',69
'RAEA',70
'INTL',71
P.FLIGHT_TYPE)72
AND P.FLIGHT_DATE >= P.APPLY_DATE73
AND (P.EXPIRY_DATE IS NULL OR P.EXPIRY_DATE >= P.FLIGHT_DATE));74
75
76
-- 查询国际不存在油价的机场77
SELECT DISTINCT a.cname content, a.airport_code378
FROM dc_flight_plan p, T_AIRPORT A79
WHERE p.ori_eng = A.AIRPORT_CODE380
AND to_char(FLIGHT_DATE, 'yyyymm') = '202507'81

AND A.AREA_CODE <> 'DOME'82
and p.flt_status not in (' 取消 ')83
AND NOT EXISTS84
(SELECT *85
FROM T_FUEL_INT_UNIT_PRICE u86
WHERE u.refueling_airport = p.ori_eng87
AND p.FLIGHT_DATE >= u.APPLY_DATE88
AND (u.EXPIRY_DATE IS NULL OR u.EXPIRY_DATE >= P.FLIGHT_DATE)); 89
90
91
92