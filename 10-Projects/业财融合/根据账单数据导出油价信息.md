---
title: 根据账单数据导出油价信息
source: 有道云笔记
imported: true
related: ["视图 V_FULE_INVOICE_RECEIPT_COSTS 的优化", "中航油：生物燃油数据; 浦东航油", "国内油价确认", "国内账单导入", "油价信息维护：每月27号检查维护"]
tags: ["油价数据", "SQL脚本", "航油系统", "账单导出"]
summary: 从账单表导出国内/国际油价的SQL查询脚本
---

```sql
SELECT t.STATION_CODE AS " 站点 ", t.RECEIPT_PRICE AS " 价格 ",
```

to_char(t.INVOICE_DATE, 'yyyy-MM') AS " ⽉份 ", t.measure_unit AS " 重量单位 ",
t.currency AS " 币种 " from T_FUEL_INVOICE t

```sql
where t.INVOICE_DATE >= to_date('2024-01-01', 'yyyy-MM-dd') and t.RECEIPT_PRICE
is not null and t.RECEIPT_PRICE > 0 and t.invoice_type = 'DOME'
GROUP by t.STATION_CODE, t.RECEIPT_PRICE, to_char(t.INVOICE_DATE, 'yyyy-MM'),
t.measure_unit, t.currency
order by t.STATION_CODE, to_char(t.INVOICE_DATE, 'yyyy-MM');
SELECT
```

t.STATION_CODE " 站点 ",7
t.RECEIPT_PRICE " 原始价格 ",8
to_char(t.INVOICE_DATE, 'yyyy-MM') " ⽉份 ",9
t.MEASURE_UNIT " 原始重量单位 ",10
t.CURRENCY " 原始货币 ",11
1 * DECODE(t.measure_unit,12
'TON',13
1000,14
EXCHANGE_UNIT(t.measure_unit,15
'KG',16
t.INVOICE_DATE,17
'MEASURE')) " 转 KG ⽐例 ",18
1 * EXCHANGE_UNIT(t.currency,19
'RMB',20
t.INVOICE_DATE,21
'CURRENCY') " ⼈⺠币⽐例 ",22
((t.RECEIPT_PRICE * EXCHANGE_UNIT(t.currency,23
'RMB',24

```
t.INVOICE_DATE,
'CURRENCY'))/ DECODE(t.measure_unit,
'TON',
1000,
EXCHANGE_UNIT(t.measure_unit,
'KG',
t.INVOICE_DATE,
```

'MEASURE'))) AS " 价格 KG/RMB"32

```sql
FROM
T_FUEL_INVOICE t34
WHERE
t.INVOICE_DATE >= to_date('2024-01-01', 'yyyy-MM-dd')
AND t.RECEIPT_PRICE IS NOT NULL
AND t.RECEIPT_PRICE > 038
AND t.INVOICE_TYPE != 'DOME'
GROUP BY
t.STATION_CODE,
t.RECEIPT_PRICE,
to_char(t.INVOICE_DATE, 'yyyy-MM'),
t.MEASURE_UNIT ,
t.CURRENCY,
1 * DECODE(t.measure_unit,
'TON',
1000,
EXCHANGE_UNIT(t.measure_unit,
'KG',
t.INVOICE_DATE,
'MEASURE')),
1 * EXCHANGE_UNIT(t.currency,
'RMB',
t.INVOICE_DATE,
'CURRENCY'),
(t.RECEIPT_PRICE * EXCHANGE_UNIT(t.currency,
'RMB',
t.INVOICE_DATE,
'CURRENCY'))/ DECODE(t.measure_unit,
'TON',
1000,
EXCHANGE_UNIT(t.measure_unit,
'KG',
t.INVOICE_DATE,
'MEASURE'))
ORDER BY
t.STATION_CODE,
to_char(t.INVOICE_DATE, 'yyyy-MM');
```

---
## 相关笔记
- [[视图 V_FULE_INVOICE_RECEIPT_COSTS 的优化]]
- [[中航油：生物燃油数据; 浦东航油]]
- [[国内油价确认]]
- [[国内账单导入]]
- [[油价信息维护：每月27号检查维护]]
