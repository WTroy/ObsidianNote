---
title: 删除重复航班数据；共享退回单据处理
source: 有道云笔记
imported: true
related: ["Mysql优化", "Mysql事务，事务的隔离级别", "MYSQL技术内幕 InnoDB存储引擎", "信创平台达梦安装", "ES虚机部署"]
---

----- 删除重复航班数据，需每天早上九点执⾏⼀次，每次执⾏当⽉⽇期
-- 查询重复航班数据1
SELECT2
*3
FROM4
dc_flight_plan k,5
(6
SELECT7
FLIGHT_DATE ,8
FLIGHT_NO ,9
CARRIER ,10
ORI_ENG ,11
DES_ENG ,12
FLIGHT_TYPE13
FROM14
dc_flight_plan15
WHERE16
to_char(flight_date, 'yyyy-MM') = '2024-02'17
AND flt_status != ' 取消 '18
GROUP BY19
FLIGHT_DATE ,20
FLIGHT_NO ,21
CARRIER ,22
ORI_ENG ,23
DES_ENG ,24
FLIGHT_TYPE25
HAVING26
count(*) > 1) k127
WHERE28
k.FLIGHT_DATE = k1.FLIGHT_DATE29
AND30
k.FLIGHT_NO = k1.FLIGHT_NO31
AND32
k.CARRIER = k1.CARRIER33
AND34
k.ORI_ENG = k1.ORI_ENG35
AND36
k.DES_ENG = k1.DES_ENG37
AND38
k.FLIGHT_TYPE = k1.FLIGHT_TYPE;39

40
-- 删除重复航班处理41
SELECT42
*43
FROM44
dc_flight_plan a45
WHERE46
(a.FLIGHT_DATE ,47
a.FLIGHT_NO ,48
a.CARRIER ,49
a.ORI_ENG ,50
a.DES_ENG ,51
a.FLIGHT_TYPE) IN (52
SELECT53
FLIGHT_DATE ,54
FLIGHT_NO ,55
CARRIER ,56
ORI_ENG ,57
DES_ENG ,58
FLIGHT_TYPE59
FROM60
dc_flight_plan61
WHERE62
to_char(flight_date, 'yyyy-MM') = '2024-02'63
AND flt_status != ' 取消 '64
GROUP BY65
FLIGHT_DATE ,66
FLIGHT_NO ,67
CARRIER ,68
ORI_ENG ,69
DES_ENG ,70
FLIGHT_TYPE71
HAVING72
count(*) > 1)73
AND rowid NOT IN (74
SELECT75
min(rowid)76
FROM77
dc_flight_plan78
WHERE79
to_char(flight_date, 'yyyy-MM') = '2024-02'80
AND flt_status != ' 取消 '81

GROUP BY82
FLIGHT_DATE ,83
FLIGHT_NO ,84
CARRIER ,85
ORI_ENG ,86
DES_ENG ,87
FLIGHT_TYPE88
HAVING89
count(*)>1);90
91
-- 返航航班处理：删除返航航班92
DELETE93
FROM94
DC_FLIGHT_PLAN dfp95
WHERE96
dfp.ID IN (97
SELECT98
id99
FROM100
dc_flight_plan k,101
(102
SELECT103
FLIGHT_DATE ,104
FLIGHT_NO ,105
CARRIER ,106
ORI_ENG ,107
DES_ENG ,108
FLIGHT_TYPE109
FROM110
dc_flight_plan111
WHERE112
to_char(flight_date, 'yyyy-MM') = '2024-02'113
AND flt_status != ' 取消 '114
GROUP BY115
FLIGHT_DATE ,116
FLIGHT_NO ,117
CARRIER ,118
ORI_ENG ,119
DES_ENG ,120
FLIGHT_TYPE121
HAVING122
count(*) > 1) k1123

98--------------- 当有单据退回时，需执⾏以下步骤 1 、 2 、 3---------------
------1. 清除账单表 apid, 执⾏时需修改参数 source_receipt_id
--------2. 清除关联预提航班 id ，执⾏时需修改参数 source_receipt_id
WHERE124
k.FLIGHT_DATE = k1.FLIGHT_DATE125
AND126
k.FLIGHT_NO = k1.FLIGHT_NO127
AND128
k.CARRIER = k1.CARRIER129
AND130
k.ORI_ENG = k1.ORI_ENG131
AND132
k.DES_ENG = k1.DES_ENG133
AND134
k.FLIGHT_TYPE = k1.FLIGHT_TYPE135
AND k.ORI_ENG = k.DES_ENG)136
update t_fuel_invoice1
set ap_invoice_id = null2
where proxyid in (select f.proxyid3
from t_fuel_invoice_ap_invoice tt4
inner join t_fuel_invoice f5
on f.ap_invoice_id = tt.proxyid6
where tt.source_receipt_id = 'HY-001-202308-415'7
and tt.notice_status = '07');8
UPDATE DC_FLIGHT_PLAN1
SET IF_MATCH = 0, LAST_UPDATE_TIME = SYSDATE2
WHERE ID IN (SELECT O.FLIGHT_ID3
FROM T_FUEL_INVOICE_WRITE_OFF O, T_FUEL_INVOICE_AP_INVOICE I4
WHERE O.AP_INVOICE_ID = I.PROXYID5
AND I.SOURCE_RECEIPT_ID = 'HY-001-202308-415'6
AND I.notice_status = '07'7
AND O.ACCRUED_AMOUNT IS NOT NULL);8

--------3. 修改 AP 发票表状态，执⾏时需修改参数 source_receipt_id 、 is_scan
update t_fuel_invoice_ap_invoice t11
set t1.notice_status = '08', is_scan = 'Y'2
where t1.source_receipt_id = 'HY-001-202308-415'3
and t1.notice_status = '07';4

---
## 相关笔记
- [[Mysql优化]]
- [[Mysql事务，事务的隔离级别]]
- [[MYSQL技术内幕 InnoDB存储引擎]]
- [[信创平台达梦安装]]
- [[ES虚机部署]]