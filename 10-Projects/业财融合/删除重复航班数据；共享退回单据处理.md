---
title: 删除重复航班数据；共享退回单据处理
source: 有道云笔记
imported: true
related: ["Mysql优化", "Mysql事务，事务的隔离级别", "MYSQL技术内幕 InnoDB存储引擎", "信创平台达梦安装", "ES虚机部署"]
---

----- 删除重复航班数据，需每天早上九点执⾏⼀次，每次执⾏当⽉⽇期

```sql
-- 查询重复航班数据
SELECT
*
FROM
dc_flight_plan k,
(
SELECT
FLIGHT_DATE ,
FLIGHT_NO ,
CARRIER ,
ORI_ENG ,
DES_ENG ,
FLIGHT_TYPE
FROM
dc_flight_plan
WHERE
to_char(flight_date, 'yyyy-MM') = '2024-02'
AND flt_status != ' 取消 '
GROUP BY
FLIGHT_DATE ,
FLIGHT_NO ,
CARRIER ,
ORI_ENG ,
DES_ENG ,
FLIGHT_TYPE
HAVING
count(*) > 1) k
WHERE
k.FLIGHT_DATE = k1.FLIGHT_DATE
AND
k.FLIGHT_NO = k1.FLIGHT_NO
AND
k.CARRIER = k1.CARRIER
AND
k.ORI_ENG = k1.ORI_ENG
AND
k.DES_ENG = k1.DES_ENG
AND
k.FLIGHT_TYPE = k1.FLIGHT_TYPE;
-- 删除重复航班处理
SELECT
*
FROM
dc_flight_plan a
WHERE
(a.FLIGHT_DATE ,
a.FLIGHT_NO ,
a.CARRIER ,
a.ORI_ENG ,
a.DES_ENG ,
a.FLIGHT_TYPE) IN (
SELECT
FLIGHT_DATE ,
FLIGHT_NO ,
CARRIER ,
ORI_ENG ,
DES_ENG ,
FLIGHT_TYPE
FROM
dc_flight_plan
WHERE
to_char(flight_date, 'yyyy-MM') = '2024-02'
AND flt_status != ' 取消 '
GROUP BY
FLIGHT_DATE ,
FLIGHT_NO ,
CARRIER ,
ORI_ENG ,
DES_ENG ,
FLIGHT_TYPE
HAVING
count(*) > 1)
AND rowid NOT IN (
SELECT
min(rowid)
FROM
dc_flight_plan
WHERE
to_char(flight_date, 'yyyy-MM') = '2024-02'
AND flt_status != ' 取消 '
GROUP BY82
FLIGHT_DATE ,
FLIGHT_NO ,
CARRIER ,
ORI_ENG ,
DES_ENG ,
FLIGHT_TYPE88
HAVING89
```

count(*)>1);90

```sql
-- 返航航班处理：删除返航航班
DELETE
FROM
DC_FLIGHT_PLAN dfp
WHERE
dfp.ID IN (
SELECT
id
FROM
dc_flight_plan k,
(
SELECT
FLIGHT_DATE ,
FLIGHT_NO ,
CARRIER ,
ORI_ENG ,
DES_ENG ,
FLIGHT_TYPE
FROM
dc_flight_plan
WHERE
to_char(flight_date, 'yyyy-MM') = '2024-02'
AND flt_status != ' 取消 '
GROUP BY
FLIGHT_DATE ,
FLIGHT_NO ,
CARRIER ,
ORI_ENG ,
DES_ENG ,
FLIGHT_TYPE
HAVING
count(*) > 1) k1123
```

98--------------- 当有单据退回时，需执⾏以下步骤 1 、 2 、 3---------------
------1. 清除账单表 apid, 执⾏时需修改参数 source_receipt_id
--------2. 清除关联预提航班 id ，执⾏时需修改参数 source_receipt_id
WHERE124

```sql
k.FLIGHT_DATE = k1.FLIGHT_DATE
AND
k.FLIGHT_NO = k1.FLIGHT_NO
AND
k.CARRIER = k1.CARRIER
AND
k.ORI_ENG = k1.ORI_ENG
AND
k.DES_ENG = k1.DES_ENG
AND
k.FLIGHT_TYPE = k1.FLIGHT_TYPE
AND k.ORI_ENG = k.DES_ENG)
update t_fuel_invoice
set ap_invoice_id = null
where proxyid in (select f.proxyid
from t_fuel_invoice_ap_invoice tt
inner join t_fuel_invoice f
on f.ap_invoice_id = tt.proxyid
where tt.source_receipt_id = 'HY-001-202308-415'
and tt.notice_status = '07');
UPDATE DC_FLIGHT_PLAN
SET IF_MATCH = 0, LAST_UPDATE_TIME = SYSDATE
WHERE ID IN (SELECT O.FLIGHT_ID
FROM T_FUEL_INVOICE_WRITE_OFF O, T_FUEL_INVOICE_AP_INVOICE I
WHERE O.AP_INVOICE_ID = I.PROXYID
AND I.SOURCE_RECEIPT_ID = 'HY-001-202308-415'
AND I.notice_status = '07'
AND O.ACCRUED_AMOUNT IS NOT NULL);
```

--------3. 修改 AP 发票表状态，执⾏时需修改参数 source_receipt_id 、 is_scan

```sql
update t_fuel_invoice_ap_invoice t
set t1.notice_status = '08', is_scan = 'Y'
where t1.source_receipt_id = 'HY-001-202308-415'
and t1.notice_status = '07';
```

---
## 相关笔记
- [[Mysql优化]]
- [[Mysql事务，事务的隔离级别]]
- [[MYSQL技术内幕 InnoDB存储引擎]]
- [[信创平台达梦安装]]
- [[ES虚机部署]]
