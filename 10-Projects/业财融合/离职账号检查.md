---
title: 离职账号检查
source: 有道云笔记
imported: true
---

系统地址：
select * from t_user t where t.ARCHIVE_FLAG = '0';1
2
select * from T_ROLE t;3
4
SELECT t1.uniform_id,5
RTRIM(XMLAGG(XMLELEMENT(e, t.role_id || ',') ORDER BY
t.role_id).EXTRACT('//text()'), ',') AS role_ids
6
FROM T_USER_ROLE t 7
INNER JOIN t_user t1 ON t.user_id = t1.proxyid 8
WHERE t1.uniform_id IS NOT NULL9
GROUP BY t1.uniform_id;10
11
-- 删除12
select * from T_USER_ROLE t where t.user_id in (select proxyid from t_user t
where t.ARCHIVE_FLAG = '0' and t.uniform_id in ('LINQIUYAN', 'LIUJING19')) for
update;
13