---
title: 近期的SQL脚本
source: 有道云笔记
imported: true
related: ["航油预提后的数据检查：每月1号上午11点", "预提期间归属问题修复的存储过程", "FOFAS系统迁移", "宋丹-预提核销检查", "油价信息维护：每月27号检查维护"]
related: ["预提期间归属问题修复的存储过程", "备份开账存储过程", "航油预提后的数据检查：每月1号上午11点", "宋丹-预提核销检查"]
---

更新预提的计算时间：
预提更新 sql ：
CREATE INDEX PRICE_ID_IDX ON FOFAS.T_FUEL_DOM_PRICE_AIRPORT (DOM_UNIT_PRICE_ID);1
-- 查询  job 的 Id1
SELECT job, what, next_date2
FROM user_jobs3
WHERE what LIKE '%FUEL_FEE_MAIN_JOB%';4
5
6
-- 更新  DBMS 任务配置7
BEGIN8
DBMS_JOB.CHANGE(9
job       => 421,10
what      => 'PKG_FUEL_FEE_ACCRUED.FUEL_FEE_MAIN_JOB(''Y'');',11
next_date => TRUNC(ADD_MONTHS(SYSDATE, CASE WHEN TO_CHAR(SYSDATE, 'DD') < 26
THEN 0 ELSE 1 END), 'MM') + 25 + 7.5/24,
12
interval  => 'TRUNC(ADD_MONTHS(SYSDATE, 1), ''MM'') + 25 + 7.5/24'13
);14
COMMIT;15
END;16
17
-- 包名称： PKG_FUEL_FEE_ACCRUED1
2
-- 包头3
PROCEDURE FUEL_FEE_MAIN_JOB(IS_FIRST_RUN VARCHAR2);4
5
-- 包体中的存储过程6
PROCEDURE FUEL_FEE_MAIN_JOB(IS_FIRST_RUN VARCHAR2) IS7
V_FIRST_DATE DATE;8
V_LAST_DATE  DATE;9
BEGIN10
IF IS_FIRST_RUN = 'Y' THEN11
-- 第⼀次执⾏：本⽉ 1 号到本⽉ 25 号12

PKG_FUEL_FEE_ACCRUED 的备份
SELECT TRUNC(SYSDATE, 'MM'), TRUNC(SYSDATE, 'MM') + 2413
INTO V_FIRST_DATE, V_LAST_DATE14
FROM DUAL;15
ELSE16
-- ⾮第⼀次执⾏：上⽉ 26 号到本⽉ 25 号17
SELECT TRUNC(ADD_MONTHS(SYSDATE, -1), 'MM') + 25,18
TRUNC(SYSDATE, 'MM') + 2419
INTO V_FIRST_DATE, V_LAST_DATE20
FROM DUAL;21
END IF;22
-- 调⽤主处理逻辑23
FUEL_FEE_MAIN(V_FIRST_DATE, V_LAST_DATE, 'YT');24
END;25
create or replace PACKAGE PKG_FUEL_FEE_ACCRUED IS1
type test_cursor is ref cursor;2
PROCEDURE FUEL_FEE_REMIND(V_CONTENT OUT CLOB);3
PROCEDURE AFTER_FUEL_FEE_REMIND(V_TIMESTAMP VARCHAR2);4
PROCEDURE UPDATE_REQUEST_AMOUNT(V_TIMESTAMP VARCHAR2);5
PROCEDURE FUEL_FEE_FOR_SOA(V_TIMESTAMP VARCHAR2);6
PROCEDURE FUEL_FEE_FOR_INTERFACE(V_TIMESTAMP VARCHAR2);7
procedure rest_accrued_write_off(v_timestamp varchar2,8
v_oucode    varchar2,9
v_msg       out varchar2);10
PROCEDURE FUEL_FEE_ACCRUED(V_TIMESTAMP VARCHAR2);11
PROCEDURE FUEL_MATCH_FLIGHT(V_FLIGHT_DATE_START DATE,12
V_FLIGHT_DATE_END   DATE,13
V_TIMESTAMPNUM      VARCHAR2);14
PROCEDURE FUEL_FEE_EXPRESS(V_TIMESTAMP VARCHAR2);15
PROCEDURE FUEL_FEE_MAIN(V_FLIGHT_DATE_START DATE,16
V_FLIGHT_DATE_END   DATE,17
V_TYPE              VARCHAR2);18
PROCEDURE FUEL_FEE_CALCULATION(V_TIMESTAMP VARCHAR2);19
PROCEDURE FUEL_FEE_OTHER(V_TIMESTAMP VARCHAR2);20
PROCEDURE FUEL_FEE_MAIN_JOB;21
PROCEDURE FUEL_INVOICE_AP_INTERFACE(V_AP_INVOICE_ID VARCHAR2,22
V_MSG           OUT VARCHAR2);23
24

BODY
执⾏命令：
PROCEDURE SHARE_SURCHARGE_AMOUNT(V_INVOICE_ID VARCHAR2,25
V_MSG        OUT VARCHAR2) ;


26
PROCEDURE PR_UPDATE_RECEIPT_INFO_JOB;27
procedure accrued_by_accounting_mouth(v_accounting_month varchar2,28
p_cursor out   test_cursor);29
procedure Merge_Receipt;30
31
PROCEDURE PRO_FUEL_INVOICE_AP_DETAIL (V_AP_INVOICE_ID VARCHAR2,32
V_MSG       OUT VARCHAR2);33
34
PROCEDURE PRO_FUEL_AP_INVOICE_RECEIPT (V_INVOICE_ID VARCHAR2,35
V_MSG       OUT VARCHAR2);36
END PKG_FUEL_FEE_ACCRUED;37
38
39
pkg_fuel_fee_accrued.sql208.91KB
DECLARE1
job number;2
BEGIN3
DBMS_JOB.SUBMIT(JOB => job,4
WHAT => 'PKG_FUEL_FEE_ACCRUED.FUEL_FEE_MAIN_JOB();',5
NEXT_DATE => to_date('2025-08-01 07:30:00', 'yyyy-mm-dd
hh24:mi:ss'),
6
INTERVAL => 'TRUNC(LAST_DAY(SYSDATE))+1  +7.5/ (24)');7
COMMIT;8
END;9


---
## 相关笔记
- [[预提期间归属问题修复的存储过程]]
- [[备份开账存储过程]]
- [[航油预提后的数据检查：每月1号上午11点]]
- [[宋丹-预提核销检查]]