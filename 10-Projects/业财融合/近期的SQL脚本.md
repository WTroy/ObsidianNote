---
title: 近期的SQL脚本
source: 有道云笔记
imported: true
related: ["FOFAS系统迁移", "备份开账存储过程", "宋丹-预提核销检查", "预提期间归属问题修复的存储过程", "航油预提后的数据检查：每月1号上午11点"]
tags: ["航油预提", "数据库", "存储过程", "SQL脚本", "预提功能", "数据库操作"]
summary: 记录航油预提相关的SQL脚本和存储过程代码。
---

更新预提的计算时间：
预提更新 sql ：

```sql
CREATE INDEX PRICE_ID_IDX ON FOFAS.T_FUEL_DOM_PRICE_AIRPORT (DOM_UNIT_PRICE_ID);
-- 查询  job 的 Id
SELECT job, what, next_date
FROM user_jobs
WHERE what LIKE '%FUEL_FEE_MAIN_JOB%';
-- 更新  DBMS 任务配置
BEGIN
DBMS_JOB.CHANGE(
job       => 421,
what      => 'PKG_FUEL_FEE_ACCRUED.FUEL_FEE_MAIN_JOB(''Y'');',
next_date => TRUNC(ADD_MONTHS(SYSDATE, CASE WHEN TO_CHAR(SYSDATE, 'DD') <
THEN 0 ELSE 1 END), 'MM') + 25 + 7.5/24,
interval  => 'TRUNC(ADD_MONTHS(SYSDATE, 1), ''MM'') + 25 + 7.5/24'
);
COMMIT;
END;
-- 包名称： PKG_FUEL_FEE_ACCRUED
-- 包头
PROCEDURE FUEL_FEE_MAIN_JOB(IS_FIRST_RUN VARCHAR2);
-- 包体中的存储过程
PROCEDURE FUEL_FEE_MAIN_JOB(IS_FIRST_RUN VARCHAR2) IS
V_FIRST_DATE DATE;
V_LAST_DATE  DATE;
BEGIN
IF IS_FIRST_RUN = 'Y' THEN
-- 第⼀次执⾏：本⽉ 1 号到本⽉ 25 号
```

PKG_FUEL_FEE_ACCRUED 的备份

```sql
SELECT TRUNC(SYSDATE, 'MM'), TRUNC(SYSDATE, 'MM') + 2413
INTO V_FIRST_DATE, V_LAST_DATE
FROM DUAL;
ELSE
-- ⾮第⼀次执⾏：上⽉ 26 号到本⽉ 25 号
SELECT TRUNC(ADD_MONTHS(SYSDATE, -1), 'MM') + 25,
TRUNC(SYSDATE, 'MM') + 2419
INTO V_FIRST_DATE, V_LAST_DATE
FROM DUAL;
END IF;
-- 调⽤主处理逻辑
FUEL_FEE_MAIN(V_FIRST_DATE, V_LAST_DATE, 'YT');
END;
create or replace PACKAGE PKG_FUEL_FEE_ACCRUED IS
type test_cursor is ref cursor;
PROCEDURE FUEL_FEE_REMIND(V_CONTENT OUT CLOB);
PROCEDURE AFTER_FUEL_FEE_REMIND(V_TIMESTAMP VARCHAR2);
PROCEDURE UPDATE_REQUEST_AMOUNT(V_TIMESTAMP VARCHAR2);
PROCEDURE FUEL_FEE_FOR_SOA(V_TIMESTAMP VARCHAR2);
PROCEDURE FUEL_FEE_FOR_INTERFACE(V_TIMESTAMP VARCHAR2);
procedure rest_accrued_write_off(v_timestamp varchar2,
v_oucode    varchar2,
v_msg       out varchar2);
PROCEDURE FUEL_FEE_ACCRUED(V_TIMESTAMP VARCHAR2);
PROCEDURE FUEL_MATCH_FLIGHT(V_FLIGHT_DATE_START DATE,
V_FLIGHT_DATE_END   DATE,
V_TIMESTAMPNUM      VARCHAR2);
PROCEDURE FUEL_FEE_EXPRESS(V_TIMESTAMP VARCHAR2);
PROCEDURE FUEL_FEE_MAIN(V_FLIGHT_DATE_START DATE,
V_FLIGHT_DATE_END   DATE,
V_TYPE              VARCHAR2);
PROCEDURE FUEL_FEE_CALCULATION(V_TIMESTAMP VARCHAR2);
PROCEDURE FUEL_FEE_OTHER(V_TIMESTAMP VARCHAR2);
PROCEDURE FUEL_FEE_MAIN_JOB;
PROCEDURE FUEL_INVOICE_AP_INTERFACE(V_AP_INVOICE_ID VARCHAR2,
V_MSG           OUT VARCHAR2);
BODY
```

执⾏命令：
PROCEDURE SHARE_SURCHARGE_AMOUNT(V_INVOICE_ID VARCHAR2,25
V_MSG        OUT VARCHAR2) ;

PROCEDURE PR_UPDATE_RECEIPT_INFO_JOB;27
procedure accrued_by_accounting_mouth(v_accounting_month varchar2,28
p_cursor out   test_cursor);29
procedure Merge_Receipt;30
PROCEDURE PRO_FUEL_INVOICE_AP_DETAIL (V_AP_INVOICE_ID VARCHAR2,32
V_MSG       OUT VARCHAR2);33
PROCEDURE PRO_FUEL_AP_INVOICE_RECEIPT (V_INVOICE_ID VARCHAR2,35
V_MSG       OUT VARCHAR2);36

```sql
END PKG_FUEL_FEE_ACCRUED;
pkg_fuel_fee_accrued.sql208.91KB
DECLARE
job number;
BEGIN
DBMS_JOB.SUBMIT(JOB => job,
WHAT => 'PKG_FUEL_FEE_ACCRUED.FUEL_FEE_MAIN_JOB();',
NEXT_DATE => to_date('2025-08-01 07:30:00', 'yyyy-mm-dd hh24:mi:ss'),
INTERVAL => 'TRUNC(LAST_DAY(SYSDATE))+1  +7.5/ (24)');
COMMIT;
END;
```

---
## 相关笔记
- [[FOFAS系统迁移]]
- [[备份开账存储过程]]
- [[宋丹-预提核销检查]]
- [[预提期间归属问题修复的存储过程]]
- [[航油预提后的数据检查：每月1号上午11点]]
